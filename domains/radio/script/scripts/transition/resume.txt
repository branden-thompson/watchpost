{{/* transition/resume: the inter-card transition back to the programme after a takeover or a [w] read ends — including an [esc] that cut one short (F-27, HUM LEAD 2026-09-05). Data: .InProgress (true when the programme is a LIVE RELAY, which kept running underneath and is therefore rejoined mid-sentence; false for a synth read, which starts its next card cleanly). ONE script for both modes: what changes is whether the listener is being returned to something already under way, which is the only thing the two modes differ by. */}}
Watchpost Radio now returns to its regularly scheduled programming{{if .InProgress}}, which is already in progress{{end}}.
