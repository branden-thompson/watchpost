{{/* transition/masthead: the station identification read on going ON AIR from standby (F-24, HUM LEAD 2026-09-05). Data: .Location (where the station broadcasts from), .Coverage (the service area, already worded — "a 50 mile radius" or "all locations") and .Providers (the data sources, joined the way a person says a list). It is deliberately LONG: the first card has to be composed and rendered before anything can be said, and the moment a listener says "go" is otherwise the moment of the longest silence. This covers that gap with something the station means. The last sentence is not decoration — it is the limitation a weather service must state. */}}
This is Watchpost Weather Radio. Broadcasting from {{.Location}} and covering weather observations, alerts, fire data, and seismic reports in {{.Coverage}}. Data is compiled from {{.Providers}}. Data may be delayed or incomplete from these providers and is not intended for life safety use.
