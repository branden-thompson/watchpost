# Debugging log — severe-alerts-modals (0.13.0)

Issues investigated during BUILD/UAT/REVIEW, with the method and the outcome. Newest last.

| # | Symptom | Method | Root cause | Outcome |
|---|---|---|---|---|
| D-1 | PTY journey flaked: a substring expected on screen was never re-sent | Frame dumps from the expect script | bubbletea's renderer diffs cells — text landing on unchanged cells is not re-transmitted; a raw-stream substring match is only honest on a whole frame | The script forces a repaint (`stty cols` round-trip) before each assertion; documented in `07-readiness/gates.md` |
| D-2 | `esc` then `w` did nothing on a real pty | Step-by-step key probes on both binaries | The terminal sends ESC then the byte and the input layer has no ESC timeout: the pair arrives fused as alt+w (pre-existing since the bubbletea v2 adoption) | `splitEscFusion` reads alt+key as esc then key (no default binding uses alt — asserted); round 4 B-08 kept Enter/Tab/Escape themselves |
| D-3 | A takeover collided with an event read mid-render | Voice-script fake with a slow render | The read's line was rendered before the takeover was admitted and started under it | The arbiter suspends the lower class; a line rendered under suspension waits for the air (`awaitAir`), and since round 4 starts under the lock (A-04) |
| D-4 | A test fake closed a channel twice under `-race` | `-race` panic trace | Two release paths | `sync.Once` |
| D-5 | Requested tints (#886800 / #7D8800) failed the AA gate (2.9 / 2.2:1) | `TestCategoryToneContrastAA` | Too bright for bold white text | HUM LEAD chose darkened same-hue values (#5E4800 / #4C5400) |
| D-6 | The alert module read one row behind the tables after a lookup (Ridgecrest for Escondido) | Read of the lookup path; a first-frame test | Index vs list: the focus moved to the first RECENT index and Details opened before the rebuilt RECENT snapshot arrived, so the index resolved to the old top row | `lookupRef`: an empty record in the looked-up location's name until the snapshot's row at the focus IS that location |
| D-7 | "No tail after Vista's fire report" (UAT) | Composition read; real-voice renders of the tail and the seismic link; a real-audio run of the whole synth source → engine → oto path with `say`-like latency | Not reproduced synthetically — every segment played to `broadcast complete`. Round 4 A-01 then showed the engine's one preview slot closing a paused read when a takeover's tone took it; HUM LEAD considers the tail resolved by that fix | `WATCHPOST_DEBUG_RADIO` now logs every segment as it reaches the air and why a cycle ended (`cycleEnded`) — the next occurrence names the segment |
| D-8 | `TestEventReadIsSuspendedByABreakingTakeover` flaked under `-race` (≈1 in 10 package runs) | Looping the package under `-race`, reasoning about the hold's real-time steps | The takeover was admitted on the read's *mark*, before its first line; later, the read's real-time hold could expire during the takeover so the read was discarded rather than resumed | The test waits for the first spoken line and runs the read's hold on a gate the test opens after the takeover completes — no real time in the sequence |
| D-9 | The severe window's frame budget rose +700 allocs after round 4 | `AllocsPerRun` per component; base/modal string diffs between the pre-round and current trees | A foreground normaliser turned the basic bright-white code `97` into 256-colour index 97: the window's header band went purple and the compositor styled every cell | The normaliser withdrawn: a bare code is ambiguous; the header box carries no tone of its own instead (B-01) |
| D-10 | `make p10` red after small edits | `dist/p10.json` diff | New symbols entered the diff scan: a package-level memo (P10-06), a complexity-18 function, a renamed cond-var loop losing its ledger row | Memo dropped (µs cost), `fgRGB` split, `awaitAir` kept its name |
| D-11 | The 80×24 frame ran to 25 lines | Line count sweep 20–100 rows | The 3-row window floor did not yield when the control row wrapped | The inset gives first, then the window shrinks to what fits (B-07) |
