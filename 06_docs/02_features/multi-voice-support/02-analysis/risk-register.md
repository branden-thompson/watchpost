# Risk register — Multi-Voice Support (0.14.0)

DISCOVER Step 4. Every risk carries a mitigation concrete enough for PLAN (anti-pattern 2). Sev = consequence if
unmitigated; Lik = likelihood from the code probe; Status = Open / Mitigated-by-design (the mitigation is a
requirement) / Accepted. IDs are `RS-n` (the harness precedent; the brief's requirements are `R-n`, and `R6` is
the standing radio gate). The brief's intake risks map: RS-1→RS-3, RS-2→RS-2, RS-3→RS-3, RS-4→RS-8, RS-5→RS-15,
RS-6→RS-1/RS-6, RS-7→RS-16. Red-team round 1 (DISCOVER) added RS-18…RS-21.

| # | Risk | Sev | Lik | Status | Evidence | Mitigation (owner phase) |
|---|---|---|---|---|---|---|
| RS-1 | **Unbounded concurrent synthesis** — no cap exists; worst case 4–5 `say`/`piper` at once; on Linux ~10 s and ~150 MB each on the alert path | **Critical** | High | Mitigated-by-design | `piper-and-platform.md` §2 | FR-12 (the rule of record): a semaphore around `Voice.Say` only, two reserved slots for the Station Director's jobs, the hand-over rendered ahead (the mid-segment Recast as one `Say`), a bound on every `Say` (PLAN batch 1, before any role work) |
| RS-2 | **Wrong voice, silently** — `say -v <unknown>` exits 0 and speaks the default while `Name()` reports the bogus name; `piperSpec` substitutes `installed[0]` with no signal | **Critical** | High | Mitigated-by-design | `piper-and-platform.md` §6; `voice-architecture.md` risk 7 | FR-7/FR-8: typed `{macos, piper}` per role, validated against the discovered list / `os.Stat` before invocation; resolution records which link won; `[S]` row (PLAN batch 1) |
| RS-3 | **Alert latency on a cold install** — `voice()` blocks under `installMu` for the whole download; two mutexes, one untimeable; **the tone itself takes this path today** (`tone()` resolves a voice for its rate — red-team B-4) | High | High | Mitigated-by-design | `piper-and-platform.md` §3 | FR-9: alert path find-only; background `ensureRoleVoices`; UI reads *installing* (PLAN batch 2) |
| RS-4 | **Broadcast cache and generation model assume one voice** — segment-only cache key, global `gen`, `s.prev`, `{{voice}}` marquee/audio divergence, the literal `tail:{{voice}}` key | High | High | Mitigated-by-design | `voice-architecture.md` A-2.1/2.2/2.4 | voice in the cache key; `renderedSeg.voice`; role-scoped wipe; one resolver for marquee and audio; fix the tail key (PLAN batch 2, with the seam refactor) |
| RS-5 | **A splice instead of a hand-over**, or a double hand-over (the gen-based one plus the scripted one) | High | Medium | Mitigated-by-design | `voice-architecture.md` A-2.3 | the hand-over is a **Source-time** decision (next segment's voice ≠ the voice that last spoke) speaking one injected script-tree line as a single `Say` — one path, no double-fire, no stale names (red-team B-3; PLAN batch 2) |
| RS-6 | **Peak RSS with several Piper processes is unmeasured**; the PCM cache (40 segments flat) thrashes with several voices | High | Medium | Mitigated-by-design (cache) / Open (RSS, measured at UAT) | `piper-and-platform.md` §2; `voice-architecture.md` risk 1 | NFR-3: measure one process's RSS on the Arch box (HUM LEAD at UAT — MVS-D-17; 0.15.0's DISCOVER input now that the resident backend is cut, E-1; the protocol in `07-readiness/perf-protocol.md` §3); the cache is bounded in bytes (40 MB, `synth.pcm.cache`); soak phase B with the numbers in `perf-protocol.md` §4 |
| RS-7 | **Station identity wrong** — no role for lead/tail; the sign-off says "the System Voice" on modern macOS; the tail script's words go stale | Medium | High | Mitigated-by-design | `voice-architecture.md` risks 2–3; `piper-and-platform.md` §5 | FR-1 Station role (OQ-14); `{{voice}}` falls back to "your correspondent" when the sentinel has no name; ship the tail script updated (PLAN batch 2) |
| RS-8 | **Config drift across OS and versions** — no migration; a single string cannot round-trip | Medium | Medium | Mitigated-by-design | `piper-and-platform.md` §6 | FR-8/NFR-5: per-namespace fields, each OS writes its own; `Radio.Validate()` at load; cross-version fixtures (PLAN batch 1) |
| RS-9 | **Setup's flat single-screen form** does not fit a role tree + tones + previews + download progress | Medium | High | Open | `voice-architecture.md` A-3.4; assumption A-9 | the HUM LEAD's mock (OQ-10); a nested list or sub-focus designed in PLAN; go-studs components first; a Setup-owned progress line replacing `VoiceNoteMsg` (PLAN batch 4) |
| RS-10 | **Preview over a live broadcast, undecked** ×6 roles | Medium | High | Mitigated-by-design | `piper-and-platform.md` §7 | MVS-D-16: duck via the Station Director (the renamed arbiter); `Audition` kept (never pausable) (PLAN batch 4) |
| RS-11 | **Maritime report: stale age, partial blocks, units, length, and the CWF text (MVS-D-14) adding a long product read** — `nws-marine` stamps `ObservedAt = now`; tides-only / buoy-only locations are the common case; SI vs screen vs marine conventions; ~45–60 s of audio | Medium | High | Mitigated-by-design | `maritime-report.md` §9 | copy the screen's exact guards; per-phrase guards + the absence line; a `units` table with "minus"; speak only the next high/low; a spoken cap; lift `seaState`/`tideTrend` to `platform/render` (PLAN batch 3) |
| RS-12 | **`Compose` signature churn** — 8 positional params → 10, reworked by two batches at once | Medium | High | Mitigated-by-design | `voice-architecture.md` risk 9; `maritime-report.md` §9.7 | the `Reports{Fire, Seismic, Maritime, Roles}` struct in batch 1, before either consumer |
| RS-13 | **Tone semantics** — a burst mixing classes; two-class products; the severe read gains a tone it never had; a preset with a different tail would change the takeover's pacing | Medium | Medium | Mitigated-by-design | `voice-architecture.md` A-3.2; `tones.md` | OQ-13/16 rulings; every preset keeps the 2 s tail; the classifier takes the product string; a narration test for the read's tone (PLAN batch 3) |
| RS-14 | **Golden churn** — removing the `[V]` control re-flows the control row and moves every frame golden; the declset pins in `app`, `synth`, `snapshot`, `render` re-capture | Low | High | Accepted | `voice-architecture.md` A-3.4; `maritime-report.md` §5 | re-record goldens once, after the Radio UI mock lands (PLAN batch 4), not per step |
| RS-15 | **Scope** — roles, seams, maritime, tones, a Radio UI redesign and a Setup redesign in one release | Medium | High | Mitigated-by-design | brief RS-5 | DATA FIRST batching: 1 cap + config + resolver → 2 broadcast/narration seams → 3 maritime + tones → 4 Setup + Radio UI; each batch UAT-able alone; the non-goals |
| RS-16 | **R6** — the broadcast path changes structurally | High | Medium | Mitigated-by-design | brief RS-7 | relay untouched; relay + audio smokes on both platforms blocking at VALIDATE; the 0.13.0 Linux half folds in; `soak_test.go`'s R6 pattern for the maritime section |
| RS-17 | **macOS voice identity is unstable** — duplicate `Aman`/`Tara` engines; Enhanced/Premium under the same name | Low | Low | Accepted | `piper-and-platform.md` §5 | keep the curation; prefer unambiguous names in the ear test; document |
| RS-18 | **Untrusted names reach the frame and the voice** — a config-supplied voice name (13 fields in 0.14.0) is displayed and late-bound into `{{voice}}`/the hand-over *after* `render.Plain`; an SGR escape in `voice = …` renders raw in today's chip (red-team C-1, probed) | High | Medium | Mitigated-by-design | `modal_chooser.go:69-74`, `source.go:293,309`, `dashboard.go:318,836` | FR-7: only the *resolved* voice's name is ever shown or spoken; NFR-6: `PlainLine` + a rune cap on every spoken/displayed name (PLAN batch 1) |
| RS-19 | **Setup overflows at 80×24 today** — `modalMax` = 12 rows, the form ≥ 19, no scroll for `modalSetup`, the focus mark can leave the screen (red-team C-2, probed) | High | High | Mitigated-by-design | `view.go:76,107-124`, `setup.go:69-90,259-285` | FR-4: usable at 80×24 (focus always on screen; collapsing groups); the 80×24 Setup golden (PLAN batch 4) |
| RS-20 | **The hand-over inherits "broken script = silence"** — an override typo turns the hand-over into the splice FR-5 forbids (red-team C-3) | Medium | Medium | Mitigated-by-design | `script/script.go:100-109,165` | FR-5: a broken hand-over override falls back to the built-in line, never silence; `.Voice` kept as an alias field (PLAN batch 2) |
| RS-21 | **A rebound `voice` key breaks startup** — `term.Merge` keeps unknown actions; a reassigned `V` errors "bound to both" (red-team A-3/C-5) | Medium | Low | Mitigated-by-design | `platform/term/term.go:120-150`, `dashboard.go:313-316` | FR-14: the `voice` action is kept and redefined as "open Setup at Correspondents" (OQ-19); removed actions in `[keys]` are ignored with an `[S]` note |
| RS-22 | **A downgrade's first save drops the role tables** — load→edit→save through the 0.13.0 struct discards unknown keys (red-team B-1, measured) | Medium | High | Accepted (for 0.13.0) / Mitigated-by-design (forward) | `app/dashboard.go:274-281,466-472`; go-toml v2 | NFR-5 reworded (a downgrade *reads* the file; its save drops the tables); 0.14.0's save preserves unknown keys so the hole ends here (PLAN batch 1) |
| RS-23 | **Resident Piper has no completion signal with `output_file`, no per-utterance cancel, no shutdown owner** (red-team B-5) | High | Medium | Deferred (0.15.0 — the resident backend is cut from 0.14.0, E-1) | upstream `main.cpp`; `voice.go:124-138` (`wavPCM` clamps a half-written WAV silently); `dashboard.go:87` | the OQ-18 Arch checklist (`perf-protocol.md` §3): prefer `--output_dir`, cancel = kill-and-respawn priced in NFR-2, a pool `Close` in `stopAll` (0.12.0 lesson: every goroutine needs a shutdown owner); previews on the app ctx (FR-4) |
