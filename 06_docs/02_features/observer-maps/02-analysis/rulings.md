---
title: "0.18.0 Observer maps — HUM LEAD rulings"
date: 2026-09-22
phase: DISCOVER (RCC)
sev: SEV-0
authority: HUM LEAD
status: "LIVE — every ruling is written here the moment it is made (D-11)."
---

# 0.18.0 — rulings

Recorded verbatim. A ruling's disposition says what it binds; a correction to a ruling is a new row,
never an edit of an old one.

| # | Date | Question | HUM LEAD verbatim | Ruling |
|---|---|---|---|---|
| D-1 | 2026-09-22 | Fold the identity-guard commit 17e40d4 into 0.18.0 or ship alone | "B fold into the 0.18.0 branch - if it's just housekeeping of documentation then it's not worth the 40m CI run on watchpost" | Folded. Corrected on record: it is ~340 lines of gate code, not docs; it is reviewed as 0.18.0 code. |
| D-2 | 2026-09-22 | Branch name | "A is fine - we work off a feature branch, then cut a squash merged release/ branch when we're ready to ship" | `feature/map-drawing`; `release/v0.18.0` squash at SHIP. |
| D-3 | 2026-09-22 | Scope and sequencing | "this will happen in 2 'macro phases' -> We'll get the maps integrated and 'drawing' -> Then we'll build out how the maps actually 'work' from a user experience perspective in watchpost (Modals, when a user can hit a map specific to the location they're looking at in details, etc)" / "We're going to focus on OBSERVER map functionality first, BROADCASTER will have 1 map ... (this will likely be 0.19.0)" | Observer only; two macro phases; Broadcaster map → F-174, likely 0.19.0. |
| D-4 | 2026-09-22 | Posture | "re-step into a LEVEL-1, SEV-0, FULL RCC, FULL PLAN ... If needed we can discuss user features, key bindings (POC used 'g' - which seems fine), minimum resolution requirements (maps are an enhancement so we can impose modal size minimums and show a 'btop' style 'minimum terminal size of <120 x 60> is required for maps,' or whatever works." | LEVEL-1, SEV-0, HUM LEAD. `g` provisionally fine; minimum-size gate acceptable, dimensions open. |
| D-5 | 2026-09-22 | Directive set | "YES - full directive set: FULL GIT; FULL DOCS; FULL REPORTS; FULL DIAGRAMS; FULL RCC; FULL PLAN; FULL TDD; FULL INST" | Full 0.16.0 set. |
| D-6 | 2026-09-22 | Problem statement | "Problem Statement A approved" | **LOCKED:** "A person watching the weather in Watchpost is told that an alert applies to a place, but cannot see where that alert actually is: whether its area covers them, stops short of them, or lies to one side, so they judge how close the danger is from zone names and distances they have to picture for themselves." |
| D-7 | 2026-09-22 | Most common use case | "As I mentioned in previous sessions: Radar/Precipitation is the most common use case (should be supported by artfacts)" | Radar/precipitation is the primary map view. Verified in artifacts: go-tuiMaps D-35 ("that w/radar will be the most common use-case for maps"), M1 scenario 3. v0.1.0 has `RadarImage` (one georeferenced image + colour table); loops (FR-37) and tile-image provider are deferred. **Watchpost has no radar imagery source today.** |
| D-8 | 2026-09-22 | Scale bounds (host overrides) | "watchpost is supposed to provide 'host overrides' so we NEVER default to global/continental scale, the largest we would consider would be 'national' scale with the US at center for now. Most commonly we'll be in 'regional/state/county' level zooms to start." | Watchpost bounds zoom: never wider than US-national, US-centred; default regional/state/county. Consequences: library has no host-settable zoom bound (MinZoom is a constant, view.go:16) and falls back to whole-world when a kept view is invalid after resize (map.go:265), so watchpost must enforce the bound (or the library gains an additive option); embedded tiles stop at zoom 3, so state/county requires the network basemap (OpenFreeMap per go-tuiMaps D-21) + disk cache. |
| D-9 | 2026-09-22 | Radar source | "there's no reason for us not to leverage both - we can also do the work in tuimaps since we own it to handle NOAA/NCEP MRMS colors, etc." | **Both:** IEM (`nexrad-n0q`, published 256-entry table) and NOAA/NCEP MRMS (`conus_bref_qcd`). The MRMS colour-to-intensity table is derived and owned in go-tuiMaps, not watchpost. Primary/fallback order to be set in PLAN. |
| D-10 | 2026-09-22 | Radar loops | "We will ABSOLUTELY need loops - we only did single image to ensure the plumbing for tuimaps worked correctly ... that's what human users expect from precip/radar maps - the other alerts (fire/quake) generally dont require loops, but we can consider in the future if an actual use-case ... comes up." | Radar/precip loops are REQUIRED for 0.18.0. Needs go-tuiMaps FR-37 (`Image.Frames`, contract §9 — additive, `NextCall` already has a frame-advance source). Fire/quake: no loops; revisit only on a real use case. |
| D-11 | 2026-09-22 | Structure across the two repositories | "A approved - this makes sense to me, and the main mitigator for the parallel is to focus HEAVILY on ensuring our artifacts and durable context medium are *rigorously* kept up to date so our context/memorory/learnings are durable across session compactions or new sessions. We should also whenever possible codify learnings into executable tests, as previous learnings have demonstrated \"the rule an agent has to remember is a rule that will eventually be passed over in a new session.\"" | **Paired releases.** go-tuiMaps v0.2.0 (loops, MRMS table, zoom bound) gets its own brief and ships first; this DISCOVER writes the host requirements v0.2.0 must meet; watchpost alert drawing may proceed on v0.1.0 in parallel; radar lands after v0.2.0 is tagged. **Standing condition:** artifacts and durable context updated rigorously at every step; learnings codified as executable tests wherever possible. |
| D-12 | 2026-09-22 | Macro phase 1 / 2 boundary | "A is fine - but let's not get too ahead of ourselves. We still need to do our full RCC / full plan , etc - we can fold this into the PLAN phases of the work." | Proposed split (phase 1 drawing: `g` window on the selected location, zoom bound, network basemap + cache, alert areas + partial ruling, radar loop after v0.2.0, fire/quake points, too-small notice, credits/NO_COLOR/ascii; phase 2 UX: map from Details, pan/zoom keys, layers/legends, Describe, settings, size tiers) accepted as an INPUT to PLAN, not a DISCOVER commitment. |
| D-13 | 2026-09-22 | Standing rule: no code in PLAN | "Complete code blocks in PLAN is not testable, so we don't write code in PLAN. It bloats the plan size. Function signatures / API examples are acceptable to illustrate design shape for both human and agentic engineers (the PLAN is for us to UNDERSTAND and FOLLOW), but avoid implementation attempts disguised as illustrations." | Binding on the PLAN for 0.18.0 and go-tuiMaps v0.2.0. |
| D-14 | 2026-09-22 | Codify D-13 as an executable check | "Seems fine" | A `make verify` gate refusing function bodies in plan documents becomes a PLAN requirement (not built in DISCOVER). |
| D-15 | 2026-09-22 | Map key | "[g] is fine." | `g` opens the map. Verified free in both keymaps on this branch (modes/tty/dashboard.go:428-462, router.go:162-225). Bound as a `defaultKeyMap` action so Help and `[keys]` overrides see it. |
| D-16 | 2026-09-22 | Minimum map size | "69x12 for minimum map modal size is fine - I think tuimaps used that as their minimum size as well" | Map body floor 69×12 cells. Confirmed: go-tuiMaps NFR-7 "usable from a 69×12-cell rectangle" (D-23), reference frames 69×12 and 149×38; equals the modal body at 80×24. Below it: a btop-style "too small" notice, reusing Broadcaster's `tooSmall`/`notice` pattern. |
| D-17 | 2026-09-22 | Approve the project brief | "A approved - brief looks complete" | Brief APPROVED as presented; committed to `01-objectives/project-brief.md`. Metrics remain PROPOSED until ruled; issue of record still open. |
| D-18 | 2026-09-22 | Metrics of success | "A, +loop smoothness is a good secondary metric" | M1–M5 ADOPTED as primary metrics; **M6 — loop smoothness** ADOPTED as a secondary metric: the radar loop holds its frame rate without stalling the rest of the Observer. All six hardened against gaming in `01-objectives/problem-statement.md` during DISCOVER. |
| D-19 | 2026-09-22 | Issue of record | "A. We had made a decision previously to use gitHub issues to track work as enhacements, and then link to them in PRs to close them." | The approved brief is the body of **issue #22** ("v. 0.18.0 - Observer Maps", label `enhancement`). The 0.18.0 PR closes it by link. go-tuiMaps v0.2.0 gets its own issue on its repository, cross-linked. |
| D-20 | 2026-09-22 | The map under `--ascii` (wave-1 synthesis §5: R-7.1 vs `TestASCIIFramesCarryNothingButASCII`, `golden_test.go:173`) | "A approved" | **Under `--ascii` the map window shows a notice in place of the picture** (maps are drawn in braille; run without `--ascii`). In macro phase 2 the `Describe` text replaces the notice — the same text the owed WP-14.19 accessibility test exercises. R-7.1 is read accordingly: `NO_COLOR` and the Monochrome/Light themes draw a readable map; `--ascii` draws an honest notice, never braille. Accepted cost: a listener whose font lacks braille gets only the notice until phase 2. |
| D-21 | 2026-09-22 | A way to turn maps off; when the map touches the network | "A with amendments: - Maps being enabled should be a user-facing 's' Setting in the settings modal; --no-map flags should only really apply to the CLI commands directly for spot reports - No cache warming accepted; well make map requests when 'g' is pressed or if the user access map functionality from the specific location details modal" | **Maps on/off is a user-facing setting in the Settings window (`s`)**, persisted in config, default on. **`--no-map` exists only on the direct CLI commands for spot reports**, not on the interactive station. **No cache warming, of any source:** map network requests (basemap, radar) start only when the listener presses `g`, or opens map functionality from a location's Details window (macro phase 2). Accepted cost: every session's first map open is a cold start (M5's 3 s cold target). |
| D-22 | 2026-09-22 | Do spot reports draw maps in 0.18.0? | "A - note is as a follow up - my main point was that that's really the only time a --no-maps flag is appropriate. Otherwise it should always be a user-facing option in settings" | **Not in 0.18.0** → F-175. Clarifies D-21 as a standing principle: **an interactive feature is switched in Settings, never by a flag; a flag is appropriate only on a one-shot CLI command** (spot reports). `--no-map` is built when spot reports can draw a map, not before. |
| D-23 | 2026-09-22 | Which alerts the map draws | "C approved - BUT - we should architect this (again) so that it's configurable, and we can give the human user the OPTIONS to set the kinds of vibility THEY want to see. We can also provide helpers/guidance/messages that alert the user when they're showing too many things at once from a network perspective. This should be standing guidance for MANY of our user-facing features and functionality: -> We (the builders) optimize for performance and ensure the code is well structured and architected -> We default to giving users choice and settings unless there's hard constraints (API request limits might be one) compelling us to not do so -> Never architect ourselves into a corner; future maintainability and extensibility should be first-clas concerns ALWAYS." | **Default: the station's locations' alerts plus the national severe events in view** (C). **Visibility is the listener's choice:** which kinds of alert and hazard layers are shown is a Settings option, not a fixed rule. **The app tells the listener when their choices cost a lot of network** — guidance, not refusal — and a scope that would exceed a hard limit (the 512-zone cap, a source's stated limit) is reported, never silently dropped (M4). **Standing principles (all user-facing features, not only maps):** (P-1) builders optimise for performance and structure; (P-2) default to user choice and settings unless a hard constraint forbids it; (P-3) never architect into a corner — maintainability and extensibility are first-class, always. |
| D-24 | 2026-09-22 | Amend the approved brief for D-21..D-23 (delta view: new R-9; R-2.2 and R-5.1 become settings-backed defaults; P-1..P-3 and F-175 in Other Considerations) | "Approved" | Amendment applied to `01-objectives/project-brief.md`; issue #22's body updated to match. |
| D-25 | 2026-09-22 | Red-team F6 (Critical): `seedZoneShapes` fetches zone geometry at station start (`app/dashboard.go:89`), contradicting D-21's "no cache warming, of any source"; FR-3.2's instrument passed it because it counts basemap requests | "A approved" | **Gate the seed on the maps setting and the first map open.** D-21 means what it says, for every source including NWS zone geometry. FR-3.2's instrument is widened to count zone fetches. Accepted cost: the first open is colder, which feeds D-29's re-derivation of M5. |
| D-26 | 2026-09-22 | Red-team A-1 (Critical): a screen-reader listener gets nothing in any surface — braille map, `--ascii` notice (D-20), spot reports excluded (D-22), `Describe` in zero requirements | "Again - we already have 'say' so in a previous session, we were unsure about the necessity of a screen reader, but for this scenario A is approved - which should largely provide some kind of aggregate of the location forecast data." | **A text description ships in macro phase 1** (FR-7.4): where the picture cannot be drawn or read — `--ascii`, no braille font, a screen reader — the window renders `Describe` data as text, an aggregate of the location's hazard and forecast facts, which the existing voice (`SayVoice`/Piper) can also speak. Acceptance is M1's own question answered from the description alone. This discharges the debt go-tuiMaps D-122 redirected here. |
| D-27 | 2026-09-22 | Red-team A-4 and A-7 (Critical): the library's hatch runs only at `NoColour` depth, so R-7.1's Monochrome/Light promise was false; colour-vision deficiency named nowhere; the library ships `ReduceMotion` and the record never mentions it | "A - we should have both, and that's why were doing the two projects in parallel to ensure tuiMaps can suppoprt the 0.18.0 functional requirements" | **Both adopted now.** Colour: watchpost hints depth from `NO_COLOR` **and** the active theme; every palette it passes must pass the library's `VisionSafe` check in watchpost's own tests; **HR-7** asks v0.2.0 for an alert pattern that does not depend on colour depth (and for Extreme/Severe to differ by more than hatch spacing). Motion: a Settings row (off / slow / normal) wired to `ReduceMotion`, a stated maximum frame rate carried from the library's NFR-21, and a still form keeping the newest frame and its age. |
| D-28 | 2026-09-22 | Red-team B1 (Critical): the brief says "never wider than the United States", the requirement says "contiguous-US bound"; Puerto Rico, Anchorage and Alaskan zones are live in the tree, and 243 of 371 measured alerts were marine | "A approved - this should work at minimum for ALL US regions / territories that would be expected to be covered by our existing (and added) APIs" | **The bound is per-region, not one rectangle.** The view is clamped to the region containing the selected location, and never wider than that region. The region set is **every US region and territory the app's APIs cover**, existing and added — not a hand-picked list — including marine areas. M2's instrument checks the drawn frame against the selected location's region, not a single longitude span. |
| D-29 | 2026-09-22 | Red-team D2/D3 (Critical): M1 has no grader, N or protocol; M5's 1 s / 3 s targets are the library's NFR applied to the host and were never derived from wave 1's own measurements | "A is fine - if we find we're coming back to these metrics again then we can re-evaluate their necessity" | **M1:** the HUM LEAD is the named grader; the protocol is written down — a fixed set of scenarios from **recorded** alerts (covering, stopping short within a few cells, partially resolved, adjacent county), presented with the alert text hidden, scored covers / stops short / lies to one side. Recorded scenarios make it repeatable; live weather does not. **M5:** marked PLAN like M6 and re-derived there from the wave-1 numbers; the clock stops at the first *complete* frame. **Standing:** a metric we keep returning to gets its necessity re-examined, not patched again. |
| D-30 | 2026-09-22 | Red-team code axis: a fail-open `HOME` path that prints a pass; `make lint-identity` not running its own new invariant; and four of six guards added an hour earlier that cannot fire (`if expr == ""` provably unreachable — which P10 Rule 5 forbids) | "A - we fix issues, even if it means unwinding something recently added if it's the foundationally correct thing to do. Sunk cost fallacies are big driver of technical and architectural debt, and now is the time to be dilligent to prevent that kind of cruft - it will make our subsequent quality passes easier and chaper" | **All three fixed** (commit below): an unreadable home returns an error; the gate recipe selects every test in `identity_test.go`, discovered rather than listed; the unreachable guards and their tests are deleted and replaced by one reachable check; the one-definition test asserts an execution rather than a mention, with a control proving a commented-out call fails. **P-4 recorded as a standing principle:** unwind recent work when the foundation is wrong. |
| D-31 | 2026-09-22 | Red-team InfoSec F1/F3/F4/F5/F8: no threat model at all (nine risks, none security or privacy); source addresses unstated as closed or free-form; caches an unexpirable record of where the listener looked, with no purge; no watchpost-side radar body cap | "A approved - fix is now in the record, if code is required we add them as tasks for BUILD" | **All in the record now:** sources are a **closed list** in 0.18.0 (a custom URL is a later ruling with its own https/public-address rules); a stated cache **retention** (max age, not only bytes) and a way to clear map and radar caches; an explicit **radar body cap** (≤1 MiB), never cached over cap; a **"what leaves the machine"** table in the brief, one row per source; and security/privacy rows in the risk register. Code follows as BUILD tasks. |
| D-32 | 2026-09-22 | Red-team docs D4 (a fork, not a finding): the record has no designer-facing artifact for a release whose output is a picture; 0.16.0 carried four mocks | "B approved - we'll wire up the modal, and we'll provide control commands on the bottom similar to our other modal patterns" | **The map's look belongs to PLAN**, ratified from **rendered specimens** at 69×12 and 149×38 once the library is a dependency — not from a hand-drawn mock of a braille picture, and not from prose (the rendered-specimen calibration). The window follows the existing modal pattern, with control commands along the bottom as other windows have. |
| D-33 | 2026-09-22 | Round 2, two reviewers' fix-first: remediation moved work across the D-12 macro-phase boundary without a ruling — D-12 puts `Describe`, settings, layers and size tiers in phase 2, while FR-7.4, FR-9.1, FR-5.8 and FR-1.9 commit them — and only two of ~40 requirements carried a phase tag, so what 0.18.0 ships was undetermined | "A" | **The boundary is re-ruled and every requirement carries its phase.** **Phase 1:** the drawing path, the text description (FR-7.4), the accessibility requirements (FR-1.8, FR-1.9, FR-7.1, FR-7.5, FR-7.6), the Settings rows the other rulings demand (FR-9.1, FR-5.8, FR-3.8), and the privacy/consent requirements (FR-3.2, FR-3.9, FR-5.7). **Phase 2:** pan and zoom, the map from the Details window, layers and legends UI, size tiers. D-12 is superseded on the boundary; its two-phase shape stands. |
| D-34 | 2026-09-22 | Round 2 business: D-20 (`--ascii` shows a notice; the description replaces it in phase 2) was superseded by D-26 (the description ships in phase 1) with no supersession row, which this log's own header requires | *(clerical, under D-26)* | **D-20 is superseded by D-26 and D-33** on the `--ascii` surface: the description ships in phase 1, so `--ascii` shows the description, not a notice awaiting a later phase. D-20's ruling that `--ascii` never draws braille stands. Recorded as a row rather than an edit, per this log's rule that a correction is a new row. |
| D-35 | 2026-09-23 | Round 3 found D-27's ratified motion row (off / slow / normal, plus a rate ceiling) narrowed in place to a boolean, because go-tuiMaps v0.1.0's `ReduceMotion` is a boolean and the cited ceiling turned out to be a flash limit — a reason self-issued rather than ratified | "tuimaps should expose some kind of controls for a host to control the animated loops of overlays since the host is also proving the data for those loops. Off/slow/normal seems like the right starting point." | **D-27's three states stand, and the library gains the control.** The host supplies the **data** — the images and API responses go-tuiMaps turns into frames — and the library owns the frames and their playback, so **the library exposes the controls**: **HR-9 widens** from "loop-rate control" to *playback control over an overlay's loop, at least off / slow / normal, settable by the host and through it by the listener*, for every looped overlay and not only radar. FR-5.8 carries the three states through to Settings; FR-5.9's ceiling is set at PLAN once the control exists. Watchpost does not re-implement playback by withholding data from the library. |
| D-36 | 2026-09-23 | M1b — the description scored with the picture hidden — was added at round-2 remediation on a reviewer's finding, not by ruling, and is now the metric the release's only screen-reader requirement is defended by | "A is fine for now" | **M1b ADOPTED as a primary metric**, alongside M1..M6 (D-18). Its protocol is M1's recorded scenarios with the picture hidden and the description shown; the grader is the HUM LEAD. "For now" is recorded: if the metric set keeps needing revisiting, its necessity is re-examined rather than patched again (D-29). |
| D-37 | 2026-09-23 | The DISCOVER phase gate: the report was presented in full, with 7 gates passing and the two remaining ones the HUM LEAD's (human approval, report published) | "Approved commit the plan and PAUSE - we'll start with a clean session after I update you." | **DISCOVER APPROVED and CLOSED.** The report is committed as the phase artefact. **PLAN is NOT opened**: work pauses here by HUM LEAD instruction and resumes in a fresh session. No PLAN artefact, branch move or further commit is made under this approval. |
| D-38 | 2026-09-23 | A full `make verify` costs 20 to 40 minutes and has never changed its result for a Markdown-only commit; ruled for both repositories in go-tuiMaps v0.2.0 D-15 | "Fast docs lane approved; commit once the gate's green", then "Go 2 build docs fast lane" | **`make verify-docs`**: under the tree lock, `tools/docslane` refuses the change unless every file changed since the last commit is Markdown (staged, unstaged, deleted and untracked alike), then every test runs without the cache, then `lint-authoring` and `lint-watermark`. Local only; CI and every other change run `make verify`. Registered in `unlisted` (gates_test.go) with its reason. The tool's own tests kill four mutations (untracked files ignored, unstaged changes ignored, the comparison dropped, one stray file allowed). |
| D-39 | 2026-09-23 | Whether watchpost 0.18.0's PLAN, paused since D-37, opens now that go-tuiMaps v0.2.0's DISCOVER has closed (go-tuiMaps D-49) | "I think we also need to do watchpost and tuimaps PLAN and likely should be done "somewhat" in parallel (they need to cross reference each other for ordering in implementation plans and to ensure architecture is correct for both)." (given while approving go-tuiMaps' PLAN approach, go-tuiMaps D-52) | **PLAN OPENS, in parallel with go-tuiMaps v0.2.0's PLAN.** The two plans cross-reference: every watchpost task that depends on a library change names the library task, and go-tuiMaps' `radar-loops/03-architecture-design/integration-map.md` holds the order and the HR↔L traces. PLAN opens with the discover report's "What PLAN inherits" list; **no code in PLAN** (D-13). |
