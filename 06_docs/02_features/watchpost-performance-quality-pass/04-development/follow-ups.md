# Post-build follow-ups (HUM LEAD, 2026-08-26 — "the first thing we tackle" after the pass)

Raised by HUM LEAD while Q5 was in build, from using `v0.9.7`. Not folded into the pass: each is a
behaviour the pass changed or exposed, and each gets its own DISCOVER before a fix.

| # | What HUM LEAD saw | Most likely cause (to confirm) | First move |
|---|---|---|---|
| F-1 | The RECENT / SEARCHED table "loads slower, and all at once": the empty state shows for a few seconds before the seeded rows appear. Actual performance is faster; it *feels* slower. | Q3's RECENT publish window (5 s, `app/pipelines.go:recentPublishCoalesce`) applies to the **seed** publish too — `startStaggered` calls `rp.publish()` for the names/zips snapshot, which now waits the full window before the first frame carries the rows (it was 50 ms). | Publish the seed snapshot at once (run the publisher's `run` directly before the schedulers start, or a window that is 50 ms until the first publish and 5 s after), pinned by a test that the seed rows are on screen within one frame of the program loop being up (UAT 5.1). |
| F-2 | Some locations no longer default to a Nearest Relay: **Carlsbad, CA; Vista, CA; Lone Pine, CA; Coachella, CA** (Coachella's broadcast is Spanish — a language preference is wanted at some point). | **Confirmed external factor (2026-08-26, Q5 gate):** weatherUSA's directory advertises 116 mounts but the relay answers **404** for most of them right now — `KEC62` (San Diego: Carlsbad's and Vista's covering transmitter), `WXJ40`, `WXL58` among them; `KZZ69` plays. `curl` reproduces it outside the app; wxradio's mounts play. The app-side question is what the deck does *after* a 404: the tune list should fall through to the next candidate transmitter inside its budget, and the pty smoke showed no state for 90 s before Synth took over. Q1's per-relay failure memo (5 min) and `mountsOf`'s host pin remain candidates for a second-order effect. | Reproduce with `WATCHPOST_LIVE=1` against each location: log which candidate transmitters `ResolveWithStatus` considered, which mounts `MountsWithStatus` returned per relay, which answered 404, and how long the deck spent before falling back. Decide the fall-through budget (skip a 404 mount at once; try the next transmitter). Then a language preference (Coachella: Spanish) as a separate, specific change request (§0.9). |

Both stay open until their own gate; the pass's remaining batches (Q6, Q7) do not touch them.
