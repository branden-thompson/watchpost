package render

import (
	"strings"
	"testing"

	"github.com/branden-thompson/watchpost/third_party/go-studs/rendering"
)

func TestL5DumpRows(t *testing.T) {
	rendering.SetColorEnabledForTest(true)
	defer rendering.ResetColorEnabledForTest()
	o := Opts{Width: 129}
	for i, l := range strings.Split(o.LocationTable(l5Rows(3), 0), "\n") {
		t.Logf("L%d %q", i, l)
	}
}
