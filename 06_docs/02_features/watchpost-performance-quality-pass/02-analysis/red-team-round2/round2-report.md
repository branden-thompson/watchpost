# Red-Team ROUND 2 — PLAN exit, plan of record v2 (single-agent)

`red-team: SHIP-WITH-CONDITIONS · single-agent · scope:feature (v2 plan + plan-report v2 vs round-1 ledger and the code the tasks name) · lenses:[Code Quality axis, PLAN lens, Perf (§1, §2.5, Q3), Safety-Critical (§0.8, §2.1–§2.6 bounds, P10 gate mechanics)]`

Standards: `_a2dh/AGENTS_a2dh.md` + `02_skills/implementation/_shared/code-authoring-standards.md` (AP-*/SN-*/SEC-*/P10-*). Tree: `go build ./... && go vet ./...` clean at `975d991`; no Go file changed since the round-1 p10 run (`git diff dbf82e1 HEAD --stat -- '*.go'` empty), so `.a2dh/p10-last-run.json` (121 findings, 121 exempted, 0 live, 7 tools RAN, 0 undeclared skips) still describes the reviewed code even though its tree hash predates the two docs commits. Not re-run: the CLI writes its run record inside the repo and this round is read-only.

## 1. Verified closed (round-1 IDs whose v2 fix is present and correct as stated)

JD-1 (§0.6, Q4a-0) · JD-2 (Q0-5 exact commands, canonical fixture) · JD-3 (§0.5 docs-touched, Q2 gate grep) · JD-4 (Q2-3 flow map + symbol test) · JD-5 (Q2-4 ADR-01..06, Vocabulary) · JD-6 (Q1-5 reason template, 0 unmatched) · JD-7 (Q3-1 `AllocsPerRun`, `TestGetTextCallersMustNotMutate`) · JD-8 (Q0 cites `dump_unix.go`; `CacheStats` stays) · JD-9 (How-to-read table) · JD-10 (per-file headers) · PH-1 (Q0-1) · PH-2 (§0.1 in-package benches) · PH-3 (CI/local on every gate) · PH-4 (§0.6 exclusion list + licences regen) · PH-6 (07-readiness snapshots) · PH-7 (six files, no gitignore step) · PH-8 (Q2-3 no numbers) · PH-9 (Q0-1) · PH-10 (§0.7, §5) · PH-11 (12 retained, "cache dir/profiles") · DQ-2 (plan-report rewritten after synthesis) · DQ-3 (Q0-7) · DQ-4 (docs-touched; Q1-6 caching.md; Q2-3 architecture.md/extending.md) · DQ-5 (`objectives.md:21` now reads "≤ 52 (plan §1 …)") · DQ-6 (§1 source column) · DQ-7 · DQ-8 · DQ-9 · DQ-10 · DQ-11 (§1 row 2 "two vmmap samples", "1,500–1,900 total (NWS ~1,000)", C2 row) · DQ-12 (tasks numbered; Q4a-1 tokens doc) · CQ-1 (§0.1 deterministic vs recorded) · CQ-2 / PA-7 (§2.3 `Config.MaxRetries`, Q1-4 drops the 0/−1 trap) · CQ-3 / PA-4 / PF-4 / PR-6 (§2.2 one retention rule, LRU not expired-first, counted; Q5a-1 "304 renewal after budget overflow") · CQ-4 (Q4a-1 golden at `TERM=xterm-256color` before 004) · CQ-5 (`RequestStats` since launch + uptime, host slots) · CQ-6 / PA-3 / BQ-4 / RT-8 / PF-5 / PR-7 (§2.4 C3 = A, threshold, per-tier phased variant, pins preserved) · CQ-8 (§0.8; single-slot `layoutFor`; `Tok()`-time precompute, no kit memo) · CQ-9 · CQ-10 (004 before 001, re-measure) · CQ-11 (refusal fixtures) · CQ-12 · CQ-13 · PA-1 / PF-3 / PR-2 (§2.3 arming rule, TTL ≤ 30 s, cleared on 2xx, four fault tests, hoisted to Q1) · PA-2 / PR-8 (§2.6 tiles, span ≤ one tile, per-tile failure) · PA-5 (upstream-first alternative, Q4b) · PA-6 (colour on, canonical fixture, `app.Stats()`) · PA-8 (`dump_unix.go`/`dump_windows.go`) · PA-9 (Q1/Q4a/Q5 resized) · PA-10 · PA-11 / RP-10 · PR-1 / IS-2 / RT-9 (both constants, scheme asserted, host pin, PLAYING gate, CHANGELOG) · PR-3 / IS-6 (`If-Modified-Since` primary; 304-able kinds; two ≤ 512 B `httpguts`-valid fields) · PR-4 / RT-4 / A11-4 (predicate incl. marquee, blink expiry, [S] ages) · PR-5 (`d.layout()` on View and `syncRecentView`) · PR-9 (directory failures negatively cached, "hit at most once per 5 min") · PR-10 (budget names the two directive entries) · PR-11 (CHANGELOG look change; no SGR memo) · PR-12 (declined, recorded) · PR-13 (5a/5b, L2-F10/F11/L1-F11/L2-F14 in §6, header prefix kept) · A11-1 (`TestFrameHonoursNoColorUnderColorTerm`, known-failing until 004) · A11-2 (per-theme overrides + `TestThemeTokenContrastAA`) · A11-6 (Q6-1 rendered-frame exclusivity) · A11-8 (Q4a-5) · A11-9 (Q0-3 `--verbose` line) · BQ-1 / RT-3 (statistic exists; see R2-1 for its well-posedness) · BQ-2 / RT-6 (Q1 network half; no "retries → threads" sentence survives) · BQ-3 / RT-13 (§0.9; compass excluded, Q6-3) · BQ-5 / RT-1 (Q0-0; sampler at 5 min) · BQ-6 · BQ-7 (`bytesNet`/`bytes304`) · BQ-9 (neg-cache cap 1,024) · BQ-10 (007 upstream-first) · BQ-11 / RT-11 (§0.3 table per change, floor re-derived) · BQ-12 (viz-off idle sample) · IS-1 / RP-8 (allow-list, `Lstat`, `watchpost/` guard, planted-file test, bounded per pass) · IS-3 (`CheckRedirect` both clients) · IS-5 (`Retry-After` int/date, clamp ≤ 5 min, never a sleep) · IS-8 (pin check, `--allow-drift`, `git apply --check`) · IS-9 (Q0-1) · SC-1 / RT-12 (10 dead dir entries deleted, file+symbol keys, 0 unmatched, p10 re-run on the kit) · SC-3 (one untagged file) · SC-4 (008/009/010) · SC-5 (4 stale + 2 one-liners + 2 tag entries; dump invariants) · SC-6 (`TestFrameAllocBudget`) · SC-7 (moot under A; phased variant "no heap, no pool") · SC-8 (every bound named: memo 16 hosts, RA ≤ 5 min, host slots, sweep ≤ 10k/1k, no kit SGR map, tile span, 304 invariant) · SC-9 · PF-1 (attributed triple) · PF-2 (predicate + 133×70 fixture) · PF-6 (three sizes; ≤ 300 if Q4b) · PF-7 (Q3-1/2) · PF-8 (body memo in Q3 — key gaps in R2-4) · RT-2 (thread bound by construction, phase schedule, `sample`) · RT-5 (counters incl. tz map, profiles excluded, 30-day extrapolation) · RT-7 (Q4a/Q4b split, ≤ 470 / ≤ 300) · RT-10 (soak-profile.md, Linux branch, warm = median of 10) · RT-15 (`ps -o`, `dist/` rebuilt).

Partially closed (listed as findings below): IS-4 (R2-3), PF-9 (R2-7), SC-2 (R2-2), BQ-8 (R2-6), IS-7 (R2-20), PH-5 (R2-15), RT-14 (R2-15).

## 2. Findings

| # | Axis/Phase/Persona | Finding | Severity | Evidence (file:line) | Simplify/Delete? | Action |
|---|---|---|---|---|---|---|
| R2-1 | Perf / Code Quality (evidence soundness) | **The M1 pass criterion is not shown to be satisfiable by a leak-free process.** Hourly post-GC `HeapAlloc` over 72 h after 6 h warm-up gives n ≈ 66; OLS slope SE = σ·24/√(Σ(t−t̄)²) ≈ 0.14·σ MB/day. The sample is taken at an arbitrary instant, so σ carries in-flight bodies (`io.ReadAll` 4.8 MB, LR-6) and the HMS parse (13.5–27.8 MB live during a parse every 10–15 min, PF-7): σ ≈ 5 MB is plausible → SE ≈ 0.7 MB/day → upper 95 % CI on a flat heap ≈ 1.4 MB/day × 30 ≈ 42 MB ≫ the 8 MB bar; even 5-min sampling (n ≈ 792, SE ≈ 0.045·σ) gives ≈ 13 MB. RT-3's "SE ≈ 0.27 MB/day" assumed σ ≈ 2 MB with no measurement behind it. Units are also mixed: "5 % of plateau (≈ 8 MB)" is 5 % of a 160 MB *footprint* applied to a *live-heap* slope (≈ 2× smaller) — 8 MB of heap is ≈ 16 MB of footprint. And the Q0 local gate "a 1 h instrumented run produces … a slope line" cannot: hourly dumps in 1 h are n ≤ 2. Autocorrelation (cache-fill ramps, daily CO-OPS/product URL churn) makes a plain OLS CI overconfident in the direction of a false pass. | Important | plan §1 row 3 (:69), §2.1 (:110-114), Q0-6 (:273), Q0 gate (:275); `live-run-findings.md:74-79`; `red-team-plan.md:337` (RT-3) | Simplify: one statistic, stated in heap units, with its detection floor derived from a measured σ | Q0: sample post-GC `MemStats` every 5 min (dump stays hourly; `counters.json` can be written by a cheap timer or the dump handler takes a `--memstats-only` form) and use the **per-hour minimum** (the leak-sensitive floor of live heap, σ ≪ instant samples); state the criterion as `upper 95 % CI × 30 d < 5 % of the post-GC heap plateau` with Newey–West/HAC or block-bootstrap SEs; have `slope.py` print n, σ, SE and the achievable floor; Q0's local gate becomes "σ measured over 1 h at 5 min and the detection floor ≤ the bar" (a computable check) |
| R2-2 | Safety-Critical / P10 gate mechanics (SC-2 class) | **`a2dh p10 check --json` is labelled "CI" but cannot run in CI.** `.github/workflows/ci.yml:20-25` runs only `make verify`, `release-matrix`, `install-test`; `make verify` (`Makefile:43`) has no p10 step; the CLI lives outside the repo (`~/Desktop/LI_PROJECTS/…/dist/a2dh`) and `.a2dh*` (the exemptions ledger and run record) is gitignored (`.gitignore:12`), so the public runner has neither the tool nor the ledger. §1's last row and every "Gate (CI): … p10 snapshot" line therefore describe a local gate; round 1's SC-2 ("P10 is not a fail-closed gate") is fixed in intent but re-mislabelled. | Important | plan §1 (:77), Q0 gate (:275), Q1 gate (:284), Q4a gate (:306); `ci.yml:20-25`; `Makefile:43`; `.gitignore:12` | N | Relabel every p10 line "Gate (local, HUM LEAD)" **or** make it fail closed for real: a `make p10` target that runs the CLI when present and **fails** (not skips) when absent, wired into `verify` locally and a pre-push hook; the CI runner stays honest ("not run here") in the build log. Track `.a2dh-p10-exemptions.yml` on the feature branch (PH-6 option 1) so exemption deltas are reviewable in PR diffs |
| R2-3 | PLAN lens / Failure Propagation (IS-4 partial) | **The priority-lane exemption is ambiguous and leaks.** §2.3 says the memo "arms … never on the priority lane" — arming only. Nothing says whether a priority-lane request (alerts 20 s, obs 90 s) *consults* a memo armed by RECENT-lane failures. Three RECENT station 5xx on distinct URLs inside one 20 s window arm the memo (≤ 30 s) and, if consulted, the next alerts tick fails fast; the Q1 fault test "one station 500 never delays alerts" cannot see this (it needs three). Worse, a 429 with `Retry-After: 300` on one RECENT obs feeds a **5-min** memo (clamp ≤ 5 min) — five minutes of dark alerts if the priority lane consults it. Also IS-4 asked that a single transport error not arm; v2 arms on any transport error (one RST on hostile Wi-Fi → 30 s fast-fail for every RECENT row) — a deliberate choice, but unrecorded. | Important | plan §2.3 (:169-172, :180-183), Q1-4 (:281); `httpx.go:138-150` (`lane(ctx)` is the only lane signal available inside `do()`); `sched.go:113-127` (rehydrate 10/20/40 s) | Simplify: the priority lane never *reads* the memo (it is the half-open probe IS-4 asked for) | Specify: memo is keyed (host) but **consulted only on the normal lane**; priority-lane requests bypass it and clear it on 2xx; `Retry-After` clamps into the memo for the normal lane only, and separately into a per-client pacing hold ≤ 30 s if NWS 429s the whole client; add fault tests "3 distinct RECENT 5xx then alerts fetch is attempted" and "one transport error on one RECENT URL delays no priority row"; record the transport-error arming rationale in §2.3 |
| R2-4 | Code Quality / Perf — body memo | **The body-memo key misses inputs that change the table bytes.** From `row()` and the section builders: `Repeat: d.radioRepeat != RepeatOff` (∞ glyph on every row — `[r]` toggle, :2418), `Playing: d.radioPlaying && d.radioKey == …` (▶ clears on stop while `radioKey` stays, :2417), the theme (`render.SetTheme`, `themes.go:134`, changes every `Tok()` tint inside cells), the RECENT window size (`windowSize` → `radioHeight` (radioMin/radioViz/state), `alertHeight`, `controlRow` line count, `numPriority`, :2192-2204), and `fireBoldMW()` (:1371, rules from config/Setup). "size · selected · recentOff · units · radioKey · loading frame" leaves a stale ∞/▶ column after `[r]`/stop, a stale palette after `[t]`, and a wrong window after `[T]`/`[v]`. | Important | plan §2.5 diagram (:220) and text (:237-240); `modes/tty/dashboard.go:2183-2185` (`opts()`), `:2192-2204`, `:2336-2372`, `:2416-2431`; `platform/render/themes.go:134` | Simplify: key the memo on a small **value struct built by `layout()`** (`layoutKey{w,h,window,days,selected,recentOff,units,frame?,radioRepeat,radioPlaying,radioKey,themeRev,fireBold}`) plus the two pointers, so every future input has one home | State the full key in §2.5; Q3-1's "body-memo invalidation table" must have a row per key input (esp. `[r]`, stop, `[t]`, `[T]`, `[v]`); note the value-receiver constraint (`func (d Dashboard) View()`) — the slot must be a pointer field allocated at construction, never package state (P10-06) |
| R2-5 | Perf / Safety-Critical — FIRMS tiles | **5° tiles change the cost model in ways §2.6 does not bound.** (a) A 25 km radius box (`fire.Bounds`, `fire.go:124-128`, ≈ 0.45°) straddles a tile edge for ≈ 20 % of locations (two tiles) and ≈ 1 % (four); "a merged span never exceeds one tile" + "`fire.Near` output byte-identical" are only both true if every overlapping tile is fetched — the rule is unstated, and the request bound is then 1–4 tiles × 2 sources per location (worst case 480/10 min vs today's ~120, `firms.go:5-7`). (b) The cache stores raw bytes; `Fetch` runs per location (`app/dashboard.go:706-718`) and calls `ParseCSV` on the whole tile body each time (`firms.go:136,146`): a peak-season 5° tile is ~10³–10⁴ VIIRS rows (MBs) parsed 60 × 2 times per 10–15 min — the HMS-spike shape SC-8 warned about, moved to a new provider. (c) FIRMS counts "larger transactions … as multiple requests"; a 5° day-1 box is not documented as multiple, but nothing in the plan measures it before the tile size is fixed. | Important | plan §2.6 (:256-262), Q5a-2 (:312), §6 L2-F2 row; `domains/fire/firms/firms.go:118-169`; `domains/fire/fire.go:124-128`; FIRMS Area API doc (5,000 tx/10 min; 1–5 day range; "larger transactions may count as multiple") | Simplify: parse once per tile — a **parsed-tile memo keyed by (tile, body hash)** inside the provider, bound = tiles in use (≤ 4 × 60), owner `firms.Provider`, test pins the bound (§0.8) | Add to §2.6: the straddle rule (fetch every tile the box touches, ≤ 4), the request bound, the parsed-tile memo with its bound/test, a byte-per-tile number from the Q0 transaction count, and a fallback tile size (2.5°) if a tile body exceeds a stated byte budget |
| R2-6 | Code Quality (evidence soundness) — Q1 gate | **"disk-write counter ≈ 2k/day pace" is not derivable from the plan's own floor.** L4-F2's 45k/day = alerts 4.3k + obs 16.8k + FIRMS 12.8k + fire/marine/forecast ~10k. A `TTL ≥ 5 min` floor keeps FIRMS (10 min TTL) and the ~10k group → ≈ 24k/day at Q1 with obs excluded; and NWS obs is served `max-age=300` per C2 (§2.2 :156) — exactly at a `≥ 5 min` floor, so obs persists too (≈ 41k/day). The comparator matters (RT concluded `≥` to keep the 5-min relay directory) and is unstated. A gate that cannot be met is waved through or blocks. | Important | plan Q1 gate (:284) "≈ 2k/day (BQ-8)", §2.2 (:129 "disk only if TTL ≥ 5 min", :156); `lens-L4:46` (L4-F2); `lens-L2:10` (obs 52 s / s-maxage 300); `red-team-plan.md:210` (BQ-8's estimate) | N | Fix the comparator (`>` 5 min, with the directory exempted by an explicit `Persist()` option if a warm directory matters) and re-derive the expected rate per kind from the L2 cadence table in §0.3's table; make the gate "≤ the derived rate ± 20 %" (FIRMS tiles at Q5 lower it again) |
| R2-7 | Perf / Business — PF-9 partial | **No publish counter exists in the counter set, yet two gates read one.** §2.1's `counters.json` list (memory tiers … tz map, MemStats) and `RequestStats` carry no publishes/h or Snapshot bytes per pipeline; §1 row 6 gates "publish ≤ the Q0-measured RECENT rate × Snapshot bytes", §2.4 re-opens C3 on "publish-coalescing failures attributable to the 50 schedulers", and Q3 measures "the final publish cadence". PF-9 is marked Fixed. | Important | plan §2.1 (:104-108), §1 (:73), §2.4 (:207-208), Q3 gate (:297); `app/dashboard.go:137-153` (publisher), `:700-705` | N | Add to `counters.json`: publishes since launch and last Snapshot bytes per pipeline (priority/RECENT), and coalesced-trigger count; the C3 threshold cites those names |
| R2-8 | Safety-Critical / Code Quality — CI gate flake | **`TestFrameAllocBudget` runs under `-race` in CI and is pinned "at the baseline value".** `make verify` → `race` (`Makefile:20-21,43`); the Go tree itself skips malloc-count assertions under the race detector (`$GOROOT/src/fmt/fmt_test.go:1526-1527` "skipping malloc count under race detector"); the CI matrix runs Linux + macOS with no tty, where the kit's per-frame `/dev/tty` probe (L5-F1) takes a different path than HUM LEAD's terminal, so counts differ per OS. Q0's "green at baseline" with a tight pin is a red gate on day one. | Important | plan Q0-5 (:272), Q0 gate (:275); `Makefile:20-21,43`; `ci.yml:16-19`; `fmt_test.go:1526` | N | Run the alloc pins in a non-race `go test` step (add `make test` to CI before `verify`, and `t.Skip` under `race.Enabled`-equivalent build tag); pin `≤ baseline × 1.05` at Q0 and the absolute budgets (≤ 6,000 / ≤ 3,300) from Q3; force `TERM=xterm-256color` + colour on inside the test as the bench does |
| R2-9 | Code Quality — sweep vs 304 (§2.2) | The 304 path "renews Expires in place, **no disk write**", so a revalidated entry's disk header `Expires` and mtime never move; the sweep's "expired > 24 h grace" and the 256 MB cap "by mtime" both evict the most-alive entries first, and a relaunch then cold-fetches them. Also the allow-list's `*.tmp` is broader than the writer's own pattern (`CreateTemp(c.dir, <hash>.cache.*.tmp)`, `cache.go:172`) | Minor | plan §2.2 (:127-130, :139-147); `cache.go:162-184` | Simplify: one temp-name regex | Define sweep expiry as `max(header Expires, mtime) + grace` and queue an `os.Chtimes` on the writer goroutine on 304 (off the request path); allow-list `^[0-9a-f]{64}\.cache\.[0-9]+\.tmp$` |
| R2-10 | Safety-Critical — Q2 vs the ledger | Exemptions are keyed by exact file path (`exemptions.go:96-105`); Q2 moves `app/radio.go` (3 file+symbol entries) and others, orphaning entries (unmatched) while their findings go live. Q2's gate line has no p10 snapshot/re-key step, unlike every other batch. Small count, but it is exactly the "0 unmatched" invariant §1 pins. | Minor | plan Q2 gate (:291); `.a2dh-p10-exemptions.yml` (app/radio.go ×3, sched.go ×3); `exemptions.go:96-105` | N | Q2-1 adds "re-key every exemption whose file moved, same count, p10 snapshot `p10-q2.json`" |
| R2-11 | Safety-Critical — tool support for "0 unmatched" | `RunResult` has no field for ledger entries that matched nothing (`p10.go:58-66`; per-finding `exempted`/`exempt_reason` only), so "0 unmatched ledger entries" (§1, Q1 gate) has no implementation named. | Minor | plan §1 (:77), Q1 gate (:284); `p10.go:29-40,58-66` | N | A 20-line `scripts/quality/p10-unmatched.sh` (ledger entries − `exempt_reason`s seen in the JSON) run with each snapshot, or an upstream `unmatched[]` field in the CLI (framework backlog) |
| R2-12 | Safety-Critical — P10 budget headroom | Q0's `dump_unix.go` needs a `for sig := range ch` signal loop (P10-02, no allowlist evidence), and after Q1 the budget sits exactly at 52 = target, leaving zero headroom for Q3's hand-written `<Placemark>` scanner, memos and Q5's memo/tile code (SC-5's shape, one batch later). | Minor | plan §1 (:77), Q0-4 (:271), Q3 (:242-246); `.a2dh-p10-exemptions.yml` (9 × P10-02 today) | N | State in §1: "new code lands with zero new exemptions; any exception is a HUM LEAD gate decision that restates the target" and design the signal loop as a bounded `select` in a goroutine that exits on ctx |
| R2-13 | Docs / How-to-read table | `P10-nn … li-A2DH 02_skills/quality/p10-*` — the rule skills live under `02_skills/implementation/p10/` (`02_skills/quality/` has no p10 dir) | Minor | plan :23; framework tree | N | Fix the path |
| R2-14 | Docs / §6 disposition (PH-5 partial) | "DISCOVER #1–#23 (ranked) — as in v1, re-sequenced" points at a superseded version; v1 §3 never carried a #→batch column (the mapping is only `discover-report.md §5`, which still says #3 → Q5). A reader of the record cannot resolve #4…#23 without `git show`. RT-14's "how to read a heap/allocs profile and a soak log" for juniors is also not in any batch. | Minor | plan §6 (:357); `discover-report.md:107-118`; `git show 82d1d47:…plan.md` §3 | N | Inline the 23 rows (# → batch) in §6; add the junior profile/soak how-to to Q7's baseline document (06-key_learnings) |
| R2-15 | Docs / plan-report accuracy | "53 Important, all Fixed in plan v2" — A11-10 (deferred to HUM LEAD), BQ-3 (deferred), CQ-7 (declined), RT-3 `SetMemoryLimit` (declined) are Important and not Fixed; "Deferred ×4" enumerates five | Minor | plan-report :97-101; red-team-plan ledger :74-83 | N | "all dispositioned (Fixed/Declined/Deferred)"; count = 5 |
| R2-16 | PLAN lens / Estimate anchoring | Q0 stays at 1 d while growing to 8 tasks: two-OS `soak.sh`, `slope.py` with CIs, a signal handler with tests on two build tags, `RequestStats` + [S] rows + plain-report line, four `bench_test.go` files, README/CHANGELOG, `soak-profile.md`, `infra-ledger.md`; PA-9 resized only Q1/Q4a/Q5 | Minor | plan §5 (:345); Q0 (:266-275) | N | Q0 ≈ 2 d, or split Q0a (counters, dump, benches) / Q0b (harness, docs) |
| R2-17 | Code Quality / Dependency completeness | `slope.py` needs a t-quantile for the 95 % CI: either scipy/numpy (unstated dependency on macOS and Arch) or an embedded t-table; nothing else in the repo is Python. HAC SEs (R2-1) make this worse. | Minor | plan Q0-6 (:273) | Simplify: one language | Prefer a Go tool (`go run ./scripts/quality/slope`) or state "stdlib-only Python 3.10+ (`statistics.linear_regression`) with an embedded t-table" |
| R2-18 | PLAN lens / R6 — mount host pin | Q1-1 "mounts accepted only on the directory's own host": Icecast `listenurl`s commonly carry `:8000` and `http`; if the pin compares `url.Host` (host:port) or the URL is now derived from `listenurl` for both relays, wxradio's HTTPS mounts (today rebuilt from `listenBase`, `directory.go:108`) could be rejected or downgraded — the test fixture serves both relays from one `httptest` host (`stream_test.go:43-46`) and would not notice. Judgment finding (wxradio's live `listenurl` shape not probed this round). | Minor | plan Q1-1 (:278); `directory.go:99-110`; `stream_test.go:43-46` | N | Pin on `Hostname()` (port-agnostic); keep wxradio URL construction unchanged; fixture with a `listenurl` on `host:8000` for both relays and a PLAYING smoke on a wxradio mount in the Q1 gate |
| R2-19 | InfoSec-adjacent (IS-7 partial) | Profiles dir/file permissions (0700/0600) requested by IS-7 are not stated in §2.1 or Q0-4 (the cache dir is 0700 today, `cache.go:66`, but `profiles/` is a new sibling written by new code) | Minor | plan §2.1 (:100-104), Q0-4 (:271) | N | Add "dir 0700, files 0600" to Q0-4's invariants/test |
| R2-20 | Code Quality — Q1/Q5 sequencing | The Q1 retention rule keys on "entries that carry validators", but validator storage arrives in Q5a-1; from v0.9.5 to v0.9.8 the rule retains nothing and the Q1 tests must plant validators by fixture. | Minor | plan §2.2 (:138-140), Q1-3 (:280), Q5a-1 (:312) | N | Move "store `Last-Modified`/`ETag` (two fixed fields)" into Q1-3; Q5 adds only the send side |
| R2-21 | Safety-Critical — §0.8 self-compliance | Bounds stated as words, not numbers/policies: `RequestStats` "fixed host slots" (how many?), neg-cache "capped at 1,024" (overflow policy?), memo "≤ 16 hosts" (overflow: drop oldest or refuse to arm?), counters lack fds (BQ-1) and the Piper voices dir (RT-5), and no h2/handshake counter although Q5a-4 "verifies from the Q0 run" | Minor | plan §2.1 (:104-108), §2.2 (:145), §2.3 (:172), Q5a-4 (:312) | N | One line each: slot count (≈ 12 + other), neg-cache = LRU drop, memo = refuse-to-arm on overflow (fail open), add `fds`, `voicesBytes`, `h2Count`, `tlsHandshakes` (via `httptrace`) |
| R2-22 | Code Quality — RP-1 mitigation | "`git diff --stat` shows only moves" cannot hold for a 1→13 file split (rename detection needs whole-file similarity) | Minor | plan RP-1 (:332), Q2-1 (:287) | N | Pin purity with a test that the sorted set of top-level declarations per package is unchanged (`go/parser` over before/after) plus `go vet` |
| R2-23 | Perf / A11 — tick predicate term | The `time.Now()` set reachable from `View()` is: `:486` (volFlash — covered), `:1196` maritimeRows (tide countdown, "Observed … ago"), `:1199` fireRows ("N h ago"), `:2004` [S] ages (covered), `:2731` marquee (covered). The term "details open ∧ time-relative label" is true for every coastal or fire location and every obs age, i.e. effectively "details open"; and the details modal's `LoadingDots` (`:1429`, `:1482`) is frame-driven, not time-driven, and not implied by `anyLoading` (`row.Loading` = provider empty or no Daily; `:1482` also fires when Daily has only today). Frame 0 degrades to `...` (A11-5), so this is a spec-clarity item, not a freeze. | Minor | plan §2.5 (:223, :227-232); `dashboard.go:486,1196,1199,1429,1482,2004,2731` | Simplify: replace the conditional term with plain `showDetails` | Predicate = `anyLoading ∨ volFlash pending/just-expired ∨ (playing ∧ detail≠"" ∧ ¬vizTicking) ∨ showStatus ∨ showDetails`; the Q3 build log's enumeration is the five lines above |

## 3. Verified CLEAN (checked, sound)

- §1 baselines vs sources: 78 MB = `brief:72`; 98–101 MB = LR-6; 116–175 MB = LR-4; 30–31 = LR-3, 32 = RT-1; 670 µs / 437 KB / 10,044 = `lens-L5:39` (Perf reproduced 677/10,043); ~94 MB/min = `lens-L1:12`; ~140 MB/min = PF-1; 1,500–1,900 total / NWS ~1,000 = `lens-L2:49,44`; ~23,000 = `lens-L2:55`; 550 ms = brief; 132/76/56 = the ledger (132 entries, 76 under `third_party/`); `objectives.md:21` already carries ≤ 52 with the plan §1 formula.
- How-to-read table: C1–C5 = discover-report §4 (:95), D1/D2 = §2 (:42,48), RS-n/OQ-n = brief, `../watchpost-cli/05-debugging/` exists; only the P10 path is wrong (R2-13).
- §0.8/§0.9 principles; §0.6 sync-script sequence (pin → temp copy → `apply --check` → apply → exclude list → atomic swap → licences → p10 → stop on first failure) is complete and ordered correctly.
- §2.2 allow-list: `.json` in the regex is the 593 legacy orphans (L4-F1) — intended; `Lstat` + regular-file + non-recursive + `watchpost/` guard hold for every `userCacheSubdir` path (`app/dashboard.go:264-275`); the 24 h grace, the 256 MB cap and the 5-min floor do not conflict (cap is the backstop; date-keyed URLs age out); `TestCacheDiskTierWarmsARelaunch` and the `{"url":"` header prefix survive.
- §2.3: `MaxRetries` on `Config` is reachable (`httpx.go:54,170-175`); TTL ≤ 30 s sits under the second rehydrate (10 + 20 = 30 s → PF-3's 31 s); "not refreshed by memoised fails" closes the ratchet; `Retry-After` clamped into the memo never sleeps in `do()`; `CheckRedirect` has a precedent (`install.go`).
- §2.4: C3 = A is consistent with every lens; the phased-variant conditions preserve PR-7's pins; the RECENT publish consolidation target (`app/dashboard.go:700-705`) exists as cited.
- §2.5: `layout()` for both `View` and `syncRecentView` (value receiver noted in R2-4); `AllocsPerRun` pin; `snapshot.Key` in `row()` (`:2417`) is real; `LoadingDots` is text-safe at frame 0.
- §2.6 tile idea itself (cache/singleflight key = tile) is topology-independent and needs no scheduler change (PA-2 holds).
- Q-tasks are test-first throughout; every gate line carries CI/local; the labels are correct except p10 (R2-2) and the alloc pin's race/tty caveat (R2-8).
- RP-8/RP-9/RP-10 mitigations match the §2 text; RP-4 names the arming rule; RP-5 names the bounded refetch.
- Round-1 refutations still hold (x/ansi drift, `compact()` in modal geometry, on-disk format tolerance, `report` warm relaunch, concurrent writers).
- P10 run record: 7 tools RAN, 0 undeclared skips, 0 live; no exemption added on the branch; Go tree identical to the recorded run.

## 4. Verdict

**SHIP-WITH-CONDITIONS.** No Critical finding. Round 1's ten Criticals are closed in v2 (every "Fixed (plan v2)" ID verified above; four are partial and appear as R2-2/R2-3/R2-6/R2-7). What remains is specification precision on text that is new in v2: the M1 statistic is right in kind but its pass criterion has never been shown feasible and its Q0 gate cannot produce it (R2-1); the p10 gate is fail-closed in intent but mislabelled CI on a runner that has neither the tool nor the ledger (R2-2); the memo's priority-lane exemption is written as an arming rule, not a consulting rule, so the alerts-never-delayed property is not yet a property (R2-3); the body-memo key and the FIRMS tile design each omit inputs that decide whether they are correct and cheap (R2-4, R2-5); two gate numbers (Q1 writes/day, Q0 alloc pin under `-race`) would go red or be waved through as written (R2-6, R2-8); one counter that two gates read does not exist (R2-7). All eight are plan-text edits with named tests; none changes the batch shape or the architecture, so BUILD Q0 can start once they are applied.

**Step 9 — convergence.** Find-rate dropped from 10 Critical / 53 Important to 0 Critical / 8 Important / 15 Minor, none architectural, all on v2's new text. A third whole-base PLAN round is **not** warranted. Recommendation: apply R2-1..R2-8 as v3 edits, then fold their re-verification into the Q0 BUILD-exit red team (default-on at SEV-0), which will also be the first round to see the measuring apparatus as code — the point at which R2-1's σ and R2-8's alloc counts become facts rather than estimates.

## 5. The single change

Replace §1's growth criterion with one whose detection floor is derived from a σ measured at Q0 (5-min post-GC samples, per-hour minimum, HAC standard errors, stated in live-heap units), and make Q0's local gate "the measured floor is below the bar" — because every other number in the pass is diffed against this one, and today it is the only target the plan cannot show a leak-free process passing.
