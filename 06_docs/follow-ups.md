# Carried follow-ups

Work that is deliberately not done yet, with enough context to pick it up cold. **This file is the
record** — a follow-up that lives only in a chat log, an agent's memory or a commit body is not
carried, it is forgotten. Each row names what, why it waits, and what would make it due.

Close a row by deleting it in the commit that does the work.

**Director / ticker redesign?** Start at
`02_features/multi-voice-support/01-objectives/README-director-handoff.md` — it names what not to
retry, and why.

## Open decisions — revisit immediately after the red team concludes

These are not scheduled work. They are questions the record cannot answer yet,
and the answer decides what the work is.

### D-1 — a zone-only alert in an area you do not track, when a radius is set

**What ships today.** `app/severe.go:scopeEvents` keeps a point-less alert when
the app is separately tracking the same alert on a location in scope, tied by
normalised alert id. "In scope" is the radius as well: `severeDeck.AlertKeysWithin`
builds the tie set from tracked locations inside it, because the RECENT table is
seeded with the fifty largest US cities and each fetches its own alerts — an
unscoped tie would hand the marquee and the spoken read to a warning a thousand
miles away. That covers every hazard for a place the listener has told the app
they care about, on every surface — window, tape, takeover and the spoken read.

The tie also refuses any id `severe.NormalizeID` does not accept as a CAP alert.
An empty key would otherwise match every point-less event that also lacked an
id, so one unidentifiable tracked alert would pull the national feed's whole
zone-only output past the radius.

**Three claims made while getting here were wrong**, corrected so the next
reader does not inherit them from `400b847`'s or `e26661f`'s messages:

- *"scopeEvents refuses to tie an event with no id"*, describing a dedicated
  branch for the empty id. That branch was inert — `NormalizeID("")` already
  returns not-ok — and it has been deleted rather than left to read as the guard
  that does the work. The guard is `NormalizeID`'s own rejection.

- *"The severe window was never broken."* It was degraded. The row reached the
  window through the location's own zone query, so the listener saw the warning
  — but without the national CAP feed's half it lost that half's detail.
- *"The tape, the takeover and the spoken alert are fed from the feed alone."*
  They are not. `tickerDeck.tapeItems` appends `laneItems(severe.LaneRows())`,
  which is how Advisories and Special Weather Statements — the two lanes the
  national feed cannot fill — reach the marquee at all.

**What it does not cover.** A zone-only alert for an area with no tracked
location. Such an alert carries no coordinates, and zones only enter the app via
tracked locations, so there is nothing to measure and nothing to match. With
ALERTS-EVENTS set to a radius it is excluded; with "All" it is shown.

**Why a UGC tie does not close it.** `domains/weather/nws/alerts.go` already
decodes `affectedZones` and already matches feed alerts to tracked locations by
zone over the full untruncated list; its output *is* `locs[].Alerts`, which is
what the id tie keys on. Carrying UGC onto `globalfeed.Event` would reach the
same answer by a longer route. This was proposed and withdrawn — recorded so it
is not proposed again.

**The two real options.**

1. **Resolve the alert's zones to geometry.** NWS serves zone polygons; cache
   them and measure the distance from the default location. Exact, and the only
   thing that truly answers "is this county within N miles". It is a new fetch,
   a new cache with its own bound and TTL, and a new failure mode when a zone
   lookup fails — closer in size to adding a provider than to a patch. Needs its
   own DISCOVER and its own scoped red team.
2. **Never drop a point-less alert when a radius is set.** One line, and safe in
   the direction that matters. It makes a filtered setup noisier by every
   zone-only product nationwide.

**What decides it: a measurement, not an estimate.** How many zone-only alerts
would a filtered setup actually gain under option 2 on a normal day and on an
outbreak day? Take it from the live national feed before choosing. Option 2 is
only unacceptable if that number is large, and nobody has counted it.

**Posture.** Shipping today's behaviour as the final answer is not on the table
(see the release-posture calibration): the listener is told about hazards where
they live, but a filtered setup is silent about a county next door.

## Open

| # | Item | Why it waits | What makes it due | Raised by |
|---|---|---|---|---|
| F-1 | **One owner for persisted config.** Six independent `Load` → mutate → `Save` owners write `config.toml`; `savePreference` calls itself "the one path" and **five** siblings bypass it (~~four~~ — corrected 2026-09-07 at 0.15.0 DISCOVER by enumeration: `setThemeHook`, `setUIHook`, `applySetup`, `saveCast`, `livePipelines.commit`). Collapse to a single serialized `config.Mutate(func(*Config))`. | The two live defects it caused are fixed (the enter-save drop, the concurrent write). The refactor itself is broader than a BUILD-exit fix. | **0.15.0, before the new feeds land.** Each new provider wants a key and a save path, and Broadcaster adds a second writer. | Red-team BUILD exit — named THE architectural risk to carry into 0.15.0 |
| F-2 | **One product classifier.** ~~Three independent substring classifiers over the same NWS product strings (`domains/severe:Classify`, `domains/radio/cast:Classify`, `app/ticker.go:tickerCategory`) and three parallel six-member taxonomies held together only by prose. Divergence is already live: "Coastal Flood Statement" is `TabNone` but `ClassStatement`.~~ **CORRECTED AT 0.15.0 DISCOVER, 2026-09-07 — the row was wrong in three specifics and the real risk is sharper. (a) The taxonomies were unified: `platform/category.Category` is the one type, and `severe.Tab*`, `tty.TickerCategory` and `globalfeed.Lane*` are all aliases of it. (b) There are TWO classifiers over product strings, not three, and they answer different questions — `cast.Classify` returns a TONE class, while `severe.Classify` and `globalfeed.LaneOf` both return a CATEGORY. `tickerCategory` was a remap, not a classifier, and is now the identity (#15). (c) The cited divergence is stale on both halves: MVS-D-57 added the Statement arm, so "Coastal Flood Statement" is `TabStatements`, and the comparison was across two different axes anyway. **THE ACTUAL RISK:** `severe.Classify` has arms for Advisory, Marine, Statement and Forecast; `globalfeed.LaneOf` has none of them and defaults to Warnings. They cannot disagree today only because `severeEvents()` — the curated national query — happens to hold nine products that all name Warning or Watch or are the one civil-emergency product. Adding one advisory to that list is a one-line, obviously-safe edit that silently lands it on the band as a Warning. Nothing checks the coupling.** | No user-visible defect from THIS divergence yet — but the adjacent one in the same function was live and shipped: #15, an evacuation order relabelled a Warning on the band, fixed in 0.14.2. | 0.15.0 scope item 1. The deliverable is the producer/consumer completeness check over the closed sets — the same shape as `TestEveryFeedLaneSurvivesTheMarqueeMap` (0.14.2), applied to `severeEvents()` against both classifiers. | Red-team BUILD exit |
| F-3 | **`app/release.go` is a network feed outside the provider seam** — its own poller and goroutine, not a `snapshot.Provider`, yet it appears in `[S]`'s host table. It is the precedent six new feeds will copy. | It works and is opt-in. Moving it is a design decision, not a fix. | **RULED OUT OF THE SEAM, HUM LEAD 2026-09-08 (FR-7.1).** *"It's a useful and common feature of many applications — so this may warrant being its own thing as an exception. It only talks to GitHub specifically to see if there's a newer release of the app."* A `snapshot.Provider` is `Domains() []string` plus `Fetch → Fragment`: location-scoped hazard data that merges into the snapshot. A version check has no domain, no location and no fragment, so forcing it behind the seam means inventing all three and making the seam mean LESS. **The named exception is the ruling, and the rule goes where a new feed's author will hit it**: `docs/extending.md` under "Rules you inherit for free", beside the add-a-provider walkthrough, and `architecture.md §1.1` — because the risk F-3 identified is PRECEDENT and prose alone does not stop a copy. *Correction 2026-09-08: this landed in architecture.md ALONE, justified in the commit by "F-3 named extending.md; that file does not exist" — a claim made after searching `06_docs/` and never `docs/`. `docs/extending.md` exists, it IS the add-a-provider page, and it was the home F-3 named for good reason.* **Bound to a single check at startup** (HUM LEAD, same ruling) — which also retires the P10 exemption for `app/release.go:start`, a row that exists precisely because the loop was unbounded by nature. | Red-team BUILD exit; ruled 2026-09-08
| F-4 | **The `[S]` window builds its tables 3–4× per render** (`modalWidth` → `statusWidth` → `statusBlocks`, then again for the fill, then again for the headline) and has no allocation pin, though it is the most expensive window in the app. | The memo now hits, so the rebuild happens about once a second rather than per tick. | A measured frame cost that matters, or the next change to that window. | Red-team BUILD exit (PERF) |
| F-5 | **Three cast roles ship unreachable.** Station, Breaking and Severe-read are config-only by MVS-D-10 — no picker, no preview, no README example — yet each multiplies the fallback matrix and the PCM cache budget. | Ratified as config-only. | **CLOSED 2026-09-08 (FR-6.3): documented as deliberately unreachable, which is the branch the requirement offered and the one MVS-D-10 already ruled for.** F-5 named three roles; there are **four** — `breaking`, `severe_read`, `standard` and `station` — because F-46's ruling flattened the window to five rows for the five things a listener picks between (`castLabel`), and `standard` stopped having a row with it. **The README was worse than silent, it was WRONG:** *"all of them are Settings rows too, so you never have to write them by hand"* — a promise the window has never kept for these four. It now names them, says they inherit, says the app is usable without ever setting one, and says WHY they have no row (a picker per role was tried and rejected — F-46). Fixed in the same pass: `update_check`'s comment still said *"hourly"*, which FR-7.1 made false an hour earlier. | Red-team BUILD exit (BQ); closed 2026-09-08
| F-7 | **`plaintext.Text` is not length-bounded.** `globalfeed` caps its own fields at 120 runes; the boundary itself has no cap. | No unbounded source reaches it today. | A new provider whose text is not already capped upstream. | Red-team BUILD exit (SEC) |
| F-8 | **CLOSED 2026-09-07 (0.15.0 DISCOVER red team) — already fixed, and the row outlived the fix.** `platform/httpx/memo.go:213` reads `a.Hostname() == b.Hostname() && originPort(a) == originPort(b)`, commented "THE PORT IS PART OF THE ORIGIN." ~~Same-origin redirects compare `Hostname()`, not `Host`.~~ A redirect to a different port on the same hostname is followed, carrying a keyed URL's path. | Proven reachable only with a hostile DNS answer or a compromised provider. | Any change to the redirect policy; or 0.15.0's new hosts. | Red-team BUILD exit (SEC) |
| F-9 | **CLOSED 2026-09-07 (`c5bcd03`, FR-4.7).**  `/debug/dump` requires POST and answers 405 with `Allow: POST` to anything else; `debugMux` is split from the listener so the routes can be exercised without binding a port, and the refusal was watched failing (GET answered 200 and wrote a dump set).  `/debug/counters` keeps its GET — it reads and changes nothing.  The soak and dump scripts, the Windows trigger string and the README moved with it.  ORIGINALLY: **`/debug/dump` is a GET with no method check.** (The other half of this row — `WATCHPOST_DEBUG_PPROF_ADDR` binding publicly — was **fixed at BUILD exit, S-2**: the variable picks a PORT now, and a non-loopback host falls back to the default.) | Opt-in, off by default, developer-only. | Before the debug server is ever documented for users. | Red-team BUILD exit (SEC) |
| F-10 | **CLOSED 2026-09-07 (0.15.0 DISCOVER red team) — fixed in 0.14.0 (`1abf27d`), carried open through two releases.** `app/seen_store.go:125` calls `capOldest()` inside `mark()`, commented "the bound holds as the store GROWS, not only when it is read." ~~The seen-store's 20,000 cap is not enforced at runtime~~ — `capOldest()` runs only at load; `mark()` prunes by the 7-day window alone. | Measured at 1,057 entries after three days — 5 % of the cap. | Never, at this rate; one line if it is ever touched. | Red-team BUILD exit (PERF) |
| F-11 | **A degraded host is no longer negative-cached**, so a provider stuck in 5xx gets the full retry ladder on every tier tick. The `failureMemo` is the only remaining brake. | A deliberate consequence of making the retry-exhausted failure a `StatusError`; the alternative dropped a failing relay directory out of retry entirely. | A provider outage long enough to be noticed as load. | Red-team BUILD exit (PERF) |
| F-12 | **Ephemeral attribution stamps remain in comments outside the swept surface** (~200 `HUM LEAD` / `UAT <date>` occurrences). `AP-HIST-01` wants the class swept, not the instance. | **RULED 2026-09-08 (FR-7.4): THEY STAY, and the premise of this row was wrong.** F-12 called them *ephemeral* and filed them under `AP-HIST-01`. They are not ephemeral: `UAT 18.2`, `UAT 65`, `UAT 26.3`, `UAT 100` and `UAT 110` all resolve to numbered rows in `06_docs/02_features/watchpost-cli/04-development/b3-uat-log.md`, and they resolve to **the exact decision the comment is about** — `UAT 18.2` is the shimmer-not-"n/a" ruling, cited by the very line whose defect was fixed today under #13, and `UAT 65` is the `detailRow` lead removal cited by `detailRow`. **Measured, not asserted: 743 numbered citations across 186 distinct numbers, 94% resolving under a strict exact matcher**, and several of the remainder resolve to RANGE headings (`| 56-57 |`) the matcher cannot parse — so the true rate is higher. `AP-HIST-01` is about history-NARRATING comments ("we used to do X"); a citation that opens the ruling is the opposite of that, and deleting 1,135 of them would remove the audit trail that makes this codebase's decisions re-findable from the code. **The count in this row was also wrong — ~200 against an actual 1,135** (731 `UAT <n>`, 268 `HUM LEAD`, 136 `UAT <date>`), which is how an estimate ages when nobody re-runs it. **NO GATE BUILT, deliberately:** a citation-resolution check must understand ranges, per-feature number namespaces and three separate logs, and a dangling citation is a documentation nuisance rather than a defect — the cost is real and the yield is not. The measurement is recorded here so the next person starts from it. | Red-team BUILD exit (CQ); ruled 2026-09-08
| F-13 | **`go-studs` upstream candidates**: a configurable gutter start on `DataTableRow` (`gutterBefore` is hardcoded `i >= 3`), and a width function that agrees with `go-runewidth` on ambiguous-width glyphs. Both are worked around in `platform/render/status_table.go`. | The workarounds are contained in one file and commented. | The next go-studs sync. | 0.14.0 M6 |
| ~~F-14~~ | ~~`TestRecastHandsOverMidSegmentAtTheSameSpot` is timing-dependent~~ — **FIXED 2026-09-05, and it was never what this entry (or its duplicate F-25) said it was.** Both concluded the recast lands at a segment BOUNDARY and the product correctly takes the `announce` path — a real timing race in the product. **Neither was right.** | **The test classified `b.texts()`, which is what Bravo was asked to RENDER.** The render goroutine works one segment ahead and PRE-RENDERS a hand-over line in case the next segment needs one; when the voice has already changed mid-segment it is not needed, `announce` skips it, and nothing is played — but the render happened and `b.texts()` recorded it. Bravo appeared to introduce itself twice and the classifier (`default: takeover = x`) kept whichever came LAST, which depended on scheduling. The product was correct both ways. **Fixed by classifying the marquee record** — `onSeg` fires only for what is written — so the pin is the rule the listener experiences (ONE introduction is heard) rather than an artefact of look-ahead. **What made it findable after three sightings did not:** replacing the lossy `takeover = x` with a COUNT, so the failure printed everything Bravo said instead of one confusing string. It reproduced on the second run. F-25's standing ask had been exactly that. **Validated:** 60 runs under `-race` with 12 spinners and 6 whole-package runs under load, zero failures; previously about one in three at suite level. | Done. | 0.14.0 BUILD, three sightings, fixed at the fourth |
| F-17 | **The listener decides how alerts are read — a Settings preference, not a constant.** **Now specified in full**: `01-objectives/read-order-design.md` (HUM LEAD, 2026-08-31) — a Director-owned lineup, "most serious, closest first" by default, an ALERTS - READ ORDER group with a row per category (sort dimensions + Max), a listener-set burst size defaulting to 5, and everything past it *diverted* with "the remaining N events … are accessible by pressing w". | **RULED AND SPLIT.** The gaps G-1…G-11 are CLOSED (R-1…R-6, `lineup-model.md`), and the bounds it replaces are gone — `capBurst`/`maxBreaking`/`breakingCap` retired at T3.1. What remains of F-17 is the listener-facing SETTING, which is **F-31**, deferred to post-Broadcaster by MVS-D-79. | Superseded: see F-31. | HUM LEAD |
| F-31 | **ALERTS - READ ORDER — the listener-facing setting, DEFERRED to post-Broadcaster (MVS-D-79, HUM LEAD 2026-09-05).** The group lets a listener rearrange category priority and the two sort dimensions within each, against one global Max. **It is fully specified and NOT built**: the shape is in `read-order-design.md`, the ratified answers to G-1…G-11 are in `lineup-model.md` (R-1…R-6), and the per-category Max and TOTAL READS rows are explicitly out (R-1). | **THE ORDERING ITSELF SHIPS IN 0.14.0** — this is only the knob. The ladder, the significance fence, the global Max and the divert count are built and pinned (T1.1–T1.4, T4.3); `category.ReadRank` is the compile-time source and `defaultBurstMax = 5` the fixed Max. Building the setting means making the planner read a listener preference where it now reads the registry, which is the substantive part and why this is a point release of its own. **Two rules the row model must be able to express before it is built** (`read-order-design.md`, MVS-D-60): `Local Area Emergency` carries a proximity WEIGHT inside its lane, and `Child Abduction Emergency` is an absolute PIN to the top — *"if the row model cannot say 'this product pins to the top', it is not expressive enough"*. Neither is built and neither is in the deferred scope's estimate yet. | After the Broadcaster UI. The HUM LEAD's reason is the weightier of the two given: a knob on hazard ordering is a knob on which hazards a listener hears, and Broadcaster is the surface that will show what a rearrangement actually does. | HUM LEAD, 2026-09-05 |
| F-32 | **`advanceTicker` rebuilds the current lane's whole tape every 300 ms to obtain one integer.** `tickerLoopLen` calls `tickerLane` — every `tapeLine`, joined, converted to runes — for `d.tickerScroll %= n`, throws the tape away, and the renderer builds it again. Measured at **68 allocations per tick, ~3.2 MB/min**, running around the clock whether or not anything is on screen. | **It now has a budget it never had** (`TestTickAdvanceAllocBudget`, 68 × 1.05): `TestFrameAllocBudget` measures `View()` only, so this was the one per-300ms cost in the app with nothing pinning it. **Both obvious fixes carry risk this should not be rushed into.** Caching the length needs invalidation over the items, width, units and ascii, and a wrong length wraps the marquee in the wrong place. Moving the clamp to render time — where the tape already exists — changes what `parkScroll` stores, which is **F-16's** ruled parked-offset behaviour and a HUM LEAD call. | With F-16's own DISCOVER, which has to model the ticker's geometry and rotation anyway. The budget stops it getting worse meanwhile. | Red team BUILD exit round 3 (PERF) |
| F-33 | **RULED 2026-09-08 (HUM LEAD), and the ruling reframes the row rather than answering it.  A relay fault is the OPERATOR'S business, not the reader's:** *"Potentially yes for Observer, NO for Broadcaster... this needs to fail in a way that grabs the human operator (or automated delegate) attention, while minimizing confusion and fallout to the listener.  Hearing 'a relay has faulted,' or some other operational error message over the air is confusing, and worse, the listener can do nothing about it."*  **The industry answer is infrastructure we do not have** — a station cuts to a pre-recorded "we are experiencing technical difficulties", and NOAA Weather Radio says "this station is currently offline, please tune to <alternate>".  **What we CAN account for is a journey:** if audio was running before the fault, an intra-card TRANSITION carries the listener from the old audio to the fallback — relay → switch location → fail → transition read → backup relay, the same shape as the ruled relay A → `[w]` read → `[esc]`/finish → transition → relay A.  **Catastrophic failovers cannot be accounted for**, and the row should stop implying they can.  So this is F-27's mechanism, not a new one, and what F-33 asked for — telling the listener the relay failed — is refused for a broadcast audience.  ORIGINALLY: **The relay-fault window is entirely inaudible.** `onSilence` and `escalate` send `tty.RelaySilentMsg` and nothing else; the reason string goes to the player's DETAIL LINE, which is displayed, never spoken. A non-sighted listener of a *weather radio* hears the audio stop, ten seconds of dead air, and then a synth report beginning with no explanation — never told the relay failed, never told alternatives existed, never told a choice was made on their behalf. | **It lands in F-27's territory, which is why it is not fixed here.** The design already owns the vocabulary — MVS-D-72's attention tone and F-24's masthead both exist to put sound across a seam — but speaking at this moment is speaking on the duck path, and F-27 has already ruled that path needs its own task and its own red team (F-D5, MVS-D-67). Inventing wording for it here would also be taking a HUM LEAD call. | With F-27's wiring. It is the same seam and the same hazard. | Red team BUILD exit round 3 (A11Y) |
| F-34 | **ANSWERED 2026-09-07 (0.15.0 B2, FR-1.4) — BY MEASUREMENT, AND THE ANSWER IS NO LOCK.** The row is accurate and its conclusion does not follow. The three touch NO mutable mastercontrol state: they call `silent()`, which reads `m` and `m.v`, and `m.v` is never written after construction — while `dip` and `takeBack` write `m.ducked`/`m.held` with `m.mu` already held. Underneath, the deck delegates one-to-one to engine methods that each take `e.mu`, on **disjoint fields**: `Suppress`/`Restore` touch `e.suppressed`; `PausePreview`/`ResumePreview`/`DropHeld` touch `e.preview`, `e.held`, `e.heldOrder`. So there is no shared mutable state to serialise. `TestTheArbiterIsRaceFreeWithoutAWrapperLock` hammers all five from ten goroutines under `-race` and finds nothing — and it was validated by planting a deliberate unsynchronised field, which it caught. **A wrapper mutex would add a lock acquisition to the path that speaks alerts and buy nothing measurable.** The test stays as the guard that catches the day those three are given state of their own. ~~`holdLine`, `resumeLine` and `dropHeld` touch the voice with no lock at all**, and are not serialised against `dip`'s `duck()` and `takeBack`'s `restore()`, which are locked. | **Carried, unverified** — recorded in the T3.10 build log and, until now, nowhere else, which by this file's own preamble means it was not carried at all. It sits on the duck path, which has the F-D5 defect history. | Before F-27 wires anything new onto that path, and before Broadcaster adds a second caller into the arbiter. | Red team BUILD exit round 3 (DOCS/HYGIENE convergence) |
| F-35 | **The `ctrl+d` window is UNUSABLE at 24 rows — not merely tail-truncated — and its prose double-wraps at EVERY width.** ~~The tail is unreachable by any key at 24 rows, at every width, in a release build — and its prose double-wraps into fragments at 80 columns.~~ **AMENDED 2026-09-07 (0.15.0 DISCOVER), measured by probe at 80, 100 and 133 columns, with scenarios PRESENT:** as opened, none of the window's own controls are visible; `nav-down` brings `INJECT AN ALERT:` into view and the next one removes it again; **the scenario labels and the `›` cursor are never visible at any focus position, by any key**; `page-down` and `nav-end` do nothing, because `modalDebug` is not among `handleNav`'s scrolling windows. The footer offers `[enter] Inject` throughout, so an operator fires a scenario they cannot see. Two corrections to the mechanism below: the defect is NOT confined to the empty-`debugScenarios()` release-build case — `handleDebugNav` moves `debug.focus` and never writes `modalScroll` at all, so a debug build with scenarios is equally stuck; and the prose fragments at 133 columns exactly as at 80, so it is a hard-wrapped string being re-wrapped rather than a narrow-terminal problem. All nav routes to `handleDebugNav`, which returns unchanged when `debugScenarios()` is empty (every release build), so `modalScroll` is never written while the scroll rail still draws ▲/▼. `debugContent` is a fixed 84 and does not shrink, so `floatModalFooter` re-wraps at 66-69: eight ragged fragments at 80. | **Both are the defect class the relay-fault window fixed and pinned THIS RELEASE, in the window next door** — `relayFaultContentFor(o)` is the shrink fix and `relayfault_test.go` has the 80×24 pin. `debug_test.go` is 96 lines, all at 133×44. What is lost is the block explaining *why the window looks empty*. | Cheap, and worth doing before the diagnostics half of F-21 makes this window user-facing. | Red team BUILD exit round 3 (A11Y) |
| F-36 | **The relay-fault window's 10-second auto-close is a hard timeout on the app's only station-tuning control, and fails WCAG 2.2.1 (Level A) outright.** `cfg.TuneRelay` has exactly one caller in the tree — this window — so the ten seconds is not "you can do this later", it is the only offer. No extend, no pause, no reset on keypress, no second route. For a slow reader, a magnification user at 80 columns, or switch input, the outcome is ALWAYS Fall-Thru and the other two rows are decorative. | **Not Critical because no hazard is lost**: takeovers are unaffected, the engine advances to the next mount by itself, and the fall-through reads the report. It is exclusion from a CHOICE, not from a warning — but it is the round-2 shape the HUM LEAD already ruled on ("a technicality is not an accommodation"). Cheapest honest fixes: reset the clock on any keypress, or make the seconds a Settings value with an "off". | **HUM LEAD call**, and it is a UX ruling: how long, and whether a key resets it. | Red team BUILD exit round 3 (A11Y) |
| F-37 | **Non-ASCII furniture survives `--ascii` in four windows that have no `--ascii` scan.** `│` ×10 and `─` ×98 in **Location Details** (the most-opened modal), `⚠` and `─` in **Alert Details**, `−` U+2212 on the Details chip, `—` in `ctrl+d` (which is on the golden test's own forbidden list), `♥` in About. Round 2 ruled `--ascii` covers *the furniture the app draws*; these are furniture. | The glyph set already has every fallback needed (`render/units.go`: `Rail`, `Dash`, `Alert`) — the gap is that only three windows (frame, setup, severe) have a non-ASCII scan. Wants the completeness shape, not seven edits: one `--ascii` render of every modal asserting no rune > 127 outside a named exemption list. Same instrument F-18 wants for tokens and F-30 got for the memo key. | With F-18's completeness test — they are the same test written twice. | Red team BUILD exit round 3 (A11Y) |
| F-40 | **A real evacuation order and its fire were both invisible.** The Brengel Fire (Vista, CA) issued an evacuation order on the morning of 2026-09-06, and the hotspot appeared in the details for NEITHER Oceanside, CA 92057 NOR Vista, CA — the two nearest tracked locations. Reported by the HUM LEAD during 0.14.0 BUILD-exit UAT, against a live event. | **Two separate questions, and neither is the 0.14.0 read path.** (a) The FIRE: why no hotspot for a fire close enough to force evacuations in a neighbouring town — a FIRMS radius, a confidence filter, a satellite-pass latency, or the detection simply not being in the feed yet. (b) The ORDER: `Evacuation Immediate` is now fetched and leads the read (C-2, this release), but that is the NWS alerts feed; a fire evacuation issued by a county or CAL FIRE may never appear there at all, which would make C-2's fix necessary and not sufficient. | **CONTEXT ADDED 2026-09-08, so the DISCOVER starts from evidence.**  The HUM LEAD saw CAL FIRE's three-level evacuation notices (Aware/Warning/Immediate, or Ready/Set/Go) for the SAME Brengel fire on Watch Duty, on a day our feeds carried nothing — so (b) is a demonstrated gap rather than a hypothesis.  **NIFC cannot close it:** WFIGS is the interagency INCIDENT record and the query asks for its whole useful surface (`IncidentName, FireDiscoveryDateTime, PercentContained, IncidentSize, FinalAcres, DiscoveryAcres, InitialResponseAcres, POOState, IncidentTypeCategory`).  Evacuation is a sheriff/OES function, not a fire-agency one, so zone status is absent from that schema rather than from our field list.  **Candidate sources, unverified — check before building:** Genasys Protect (formerly Zonehaven), the platform most CA counties publish zone STATUS to and what the aggregators surface; county sheriff/OES feeds (Nixle, Everbridge, county GIS), authoritative but one integration per county; and CAL FIRE's own incidents API (`incidents.fire.ca.gov`), CA-specific and often fresher than WFIGS, though its evacuation content is narrative rather than structured.  **Two questions come before any code, and the first is not technical:** whether a documented public API exists whose terms permit redistribution *in an app that speaks the data aloud*; and what we do about staleness, since a lifted order still being announced is worse than silence and Watch Duty carries that risk with human review we will not have. | **Its own DISCOVER, against the fire data and the APIs.** Deliberately NOT a BUILD-exit item (HUM LEAD, 2026-09-06): it needs the feeds examined rather than the reader, and guessing at it from one sighting is how the wrong thing gets built. The sighting is recorded here so the investigation starts from a real event with a date and a name. | **DISPOSITIONED 2026-09-08 as FR-10 — a bounded investigation of the FEEDS, with a written outcome, and BOTH halves conclude "cannot be fixed here."** **FR-10.1 (no hotspot):** three candidates eliminated with evidence — the confidence filter cannot lose a detection (`firms.confidence` falls back to `nominal`; HMS hardcodes `analyst`), the 25 km ring covers Vista from Oceanside, and `Keep` explicitly retains an unknown FRP. What survives is structural: **FIRMS is configured for `VIIRS_NOAA20_NRT` and `VIIRS_NOAA21_NRT` — polar orbiters, ~2 passes a day each — and there is no geostationary near-real-time path**, because GOES reaches this app only through HMS, behind analyst curation. A morning ignition can be invisible for most of a day. Not a defect; a coverage boundary. **FR-10.2 (no order):** every alert comes from three endpoints — `api.weather.gov/alerts/active`, `earthquake.usgs.gov`, `nhc.noaa.gov`. An evacuation ordered by CAL FIRE, a county OES or a sheriff is not an NWS product and **will never appear there**, which makes C-2's `Evacuation Immediate` fix necessary and not sufficient, exactly as predicted. This also settles the fire-UAT question: **the CAL FIRE three-tier scheme needs a NEW SOURCE**, a 0.16.0 provider under the FR-7.1 precedent. **FR-10.3 (say so):** already shipped — every report head carries the notice and the fire/seismic heads name their providers per read. The breaking takeover carries none, RULED by the HUM LEAD 2026-09-08: *"Takeovers are brief — name the location, and direct the user or listener to a detailed report or official website"*, which `burst-closing` already does. Full analysis: `0.15.0.../02-analysis/fr10-hazard-coverage.md`. | HUM LEAD, UAT 2026-09-06; dispositioned 2026-09-08
| F-44 | **The PTY journey's severe-window block could not fail, and the one honest step in it is the one that failed.** Four assertions matched strings the DASHBOARD draws: the ticker band names its lane in words (0.14.0), and those words are the category labels — `platform/category/category.go` gives BandLabel `"Emergency Orders"` (:83), `"Warnings"` (:85), `"Watches"` (:87), `"Disasters"` (:92). A fifth, `Showing 1-`, was written to prove the Settings modal had CLOSED and is drawn by the dashboard's recent table (`modes/tty/body.go:302`) beside the open modal. Found in REVIEW, 2026-09-06, chasing a `space`-read failure. The evidence is the code, NOT a failing screen: `step` logs the tail of `expect_out(buffer)`, which is not a frame, and expect leaves the previous contents in place when an attempt reads nothing — a dump timed 20:02 carried the first-run Setup form closed at 20:01. I read one as a screenshot before checking that. | **FIXED, and it uncovered a second defect under it.** The anchors are now window-only: `Total Category Events` (`severe.go:391`, drawn on every tab including empty ones and nowhere else) and the pointer form `› *Warn` the open tab wears. That exposed the assumption underneath — `severeOpeningTab()` returns `lastBreakingTab` when a breaking event landed recently (`severe.go:166`), so on live feeds the window opens on ANY tab, and the arrows wrap (`:218`/`:220`), so counting Rights from an assumed Warnings was never sound. A `tabTo` proc presses Right until the wanted tab is selected, at most once per tab. The label guard was taught the new call shape — without that it would have gone blind to three patterns, the same way the `seeAfter` conversion silently dropped sixteen — and its count check was split into `extracted` vs `checked`, because counting after exemptions meant every honest exemption eroded the tripwire that detects a blind reader. | **STILL OPEN, and deliberately not papered over.** (a) The read step needs its tab to HAVE a focused event and nothing establishes that; an empty Warnings makes it fail as a data condition. Proving it needs an assertion about what is NOT on screen, and this harness has no honest absence check — it cannot force a dependable redraw, so "the pattern did not arrive" and "the pattern is not there" are one observation. A tolerance would put the step straight back in the class the block was just taken out of. (b) One golden rendered on a non-default severe tab would retire both `journeyUncovered` exemptions added here. (c) **The `space` read now has a real signal, and it points somewhere.** With the honest anchors the severe window demonstrably opens and all three tabs move (run of 2026-09-06 20:01), so the failure is NOT a stuck modal — it is the read. It has failed 4 of 5 runs on this machine. **RUN WITH `WATCHPOST_DEBUG_RADIO`, AND THE APP ANSWERED IT** (2026-09-06 20:08, the only failing step in that run). The read is ADMITTED at 20:08:23.998 (`narrate:admit:read:Severe-event read`) and the first audio clip starts at 20:08:38.243 — **14.2 seconds later** — with nothing logged in between; the journey's `esc` cancels it at 20:08:45. So the step is not failing on a bad pattern or a lost keypress: `▶ PLAYING` is drawn only when `radioState == "playing"` (`modes/tty/radio_panel.go:385`), which follows the PLAYER, so the panel is telling the truth for those 14 seconds. **Two things fall out, both for the HUM LEAD.** (1) There is no budget for this path: NFR-2/M4 covers the BREAKING tone (≤ 250 ms) and says nothing about a `[space]` event read. (2) For 14 seconds after the keypress the panel reads `■ STOPPED` with no title — press space, get nothing — which is F-41's shape ("silent while it works") on another surface. **The journey's bound must NOT be widened to absorb this**: that would convert a measured 14 s into a green tick. This is F-43's sibling and the trace added in `c7ace52` is what produced the number. **THE 14.2 s IS PROVISIONAL AND MUST BE RE-MEASURED: it was taken on a volume with 1.2 GiB free of 926 GiB** (274 GB of Go build cache, cleared the same day — see `quality-observations.md`, "The disk was the instrument, and it was full"). Every journey run that afternoon shares that contamination. **HUM LEAD RULINGS, 2026-09-06:** (1) re-measure after the disk is clean, and make cleanup a deterministic step rather than a threshold — done, `make hygiene`, wired into `mutant-check`. (2) The panel feedback question is **DEFERRED**, explicitly: *"until I or a real user actually experiences a 14s delay consistently. Then we can devise notification / loading tactics to make that wait either less or less apparent through transition reads."* So no budget is set and no loading state is built for 0.14.0; the trigger for reopening is a consistently observed delay in real use, not a number from a contaminated run. **RE-MEASURED ON THE CLEAN DISK (2026-09-06 21:00, 273 GiB free): admit 21:00:20.737 -> first clip 21:00:32.155 = 11.4 s**, against 14.2 s on the full one. So the disk was worth about 2.8 s and **the remaining 11.4 s is real** — but that run also had the space read as its ONLY failure, where the contaminated runs failed the watchlist, Lookup and Details steps too. Those three were the disk; this one is the app. Nothing is being done about it under ruling (2); the number is recorded so that when a real user does report the wait, the starting point is a measurement rather than a reconstruction. | agent, REVIEW 2026-09-06 |
| F-45 | **23 P10 ledger rows still name a gate they are waiting for, at gates that passed releases ago.** Enumerated at 0.14.0 REVIEW exit: the ledger holds 127 rows; 23 carry "ratify at gate", "ratify at B1a gate" or "presented for ratification" and no ratification of any wording — `platform/sched` (4), `pkg/schema` (4), `domains/fire/{hms,wfigs,firms}`, `platform/tz` (2), `domains/marine/coops`, `domains/locations/openmeteo`, `modes/report`, `domains/radio/stream`, `platform/snapshot`, `tools/alertrec`, `tools/slope`, `cmd/watchpost`, `platform/render/themes.go`, `platform/config`. | **Found by ENUMERATING rather than reading the closing line.** `director-build-log.md` asserted "the ledger is now fully ratified … none names a gate it is waiting for"; that was true of the 15 rows in its own table and false of the file — the same shape as the `tree_hash` column and the NFR-8 grep recorded green while failing. The claim is corrected in place. **Pre-existing debt, not 0.14.0's:** every row this release added or changed carries an explicit ratification with a date, so the release-checklist row is ticked against this release's scope and says so. | **DELIBERATELY NOT SWEPT INTO THE 2026-09-06 RATIFICATION.** The HUM LEAD ratified what was presented; folding in 23 rows they were never shown would be self-approval wearing someone else's signature (the standing rule: exemptions are presented, never self-approved). Wanted: the 23 presented as their own list — most are the same pure-helpers P10-05 shape already ratified elsewhere, so it is likely one reading rather than 23 — and a check that fails when any row names a gate that has passed, so this cannot silently accrue again. **CLOSED — the 23 were presented as their own list and RATIFIED by the HUM LEAD 2026-09-06 ("Ratified"), which is why they were kept out of the earlier answer.** Each row now carries the ratification in the ledger itself rather than only in a release document, so the next audit reads it from the file: **127 rows parse, 0 malformed, and 0 now name a gate they are waiting for** (58 carry an explicit ratification, up from 35). Still wanted, and not built: the check that fails when a row names a gate that has passed. **BUILT 2026-09-08 (FR-7.3), and it found five more on its first run.** `scripts/quality/ledger-ratified.sh`, wired into `p10` (the real check) and `gate-controls` (its self-test, which carries its own fixture and so runs where the ledger — `.gitignore`d, R2-2 — does not exist). **It is deliberately NOT "no row may say ratify at gate":** that grep fails on 33 of the 127 rows, every one of them ratified and merely keeping its original wording, and a gate that fires on 33 rows nobody can act on is a gate that gets switched off. The obligation is the PAIRING — a row may say where it will be presented, or record that it was, never the first without the second — and it ships with two controls, one per direction. **The five it caught were leaning on each other:** `platform/geo`, `platform/astro`, `domains/marine/ndbc`, `domains/radio/spectrum`, `domains/fire`, each citing *"the same RATIFIED pattern as platform/geo"* — another row's approval, not its own — with `platform/geo` itself among the five, so the citation chain had no ratified root. A hand audit run minutes earlier had reported ZERO, because the bare word "ratified" appears in every one of those citations. **RATIFIED as a set by the HUM LEAD 2026-09-08**, recorded in each row. | agent, REVIEW 2026-09-06; gate built 0.15.0 B7
| F-46 | **The mock's *All Reports* picker is absent from the shipped Settings window, and the role it sets is reachable only by editing the config file.** `cast.go` still declares the level — `All -> Standard -> {Weather, Maritime, Fire, Seismic, Station}` — and `cast.Groups()` returns `{Alerts, Standard}`, the mock's TWO group pickers. Settings draws five rows (`castLabel`) and none of them is Standard. | **Specified in four places and dropped in one.** The mock (`mocks/setup.md`: `Alerts / Takeovers` AND `All Reports`), PLAN §224, FR-8 (*"the Station, Breaking and Severe-read pairs are config-only — they inherit Alerts / All Reports"*) and `p4-ui.md:152` (*"an override inherits from **All Reports**, not the root"*); the prior-art code even had `focusCastStandard // All Reports picker`. Residue proves it was dropped rather than redesigned: **`roleStandard` in `setup_rows.go:177` is declared and referenced nowhere** (AP-DEAD-01), and `cast.Groups()` has no production caller. Found reviewing the goldens against the mocks, 2026-09-06. | **User-visible, and it is the CHANGELOG's own promise:** *"one voice for everything, or a different voice for the alerts than for the reports."* Setting "the reports" now means setting four rows one at a time — and even then **Station still inherits Standard, so the station reads keep the ROOT voice** while the four reports changed. `[radio.voices.standard]` is documented in the README and settable only by hand. **Wanted: a ruling** — restore the row (the tree, the config key and `Groups()` are all still there for it), or ratify the flattened design and then delete `roleStandard`, retire `Groups()`, and fix the CHANGELOG/README wording to match what the UI can actually do. **CLOSED — NOT A DEFECT. The shipped window is the record; the spec is the stale artefact.** HUM LEAD, 2026-09-06: *"That's residual spec that wasn't changed as a result of UAT; the settings record reflects the truth, which was discovered after trying the 'inheritance' model — it was too complicated and the same result could be had by just making the controls easier and giving the user fine-grained controls outright, which is what we have now."* Done under that ruling: `roleStandard` and `cast.Groups()` deleted (the residue that made this look like an omission rather than a decision); the CHANGELOG and README rewritten to describe five rows each naming the voice it inherits, instead of the retired alerts-vs-reports split they still promised; `mocks/setup.md` and `p4-ui.md` Task 4.6 marked SUPERSEDED at the head, the latter because its line *"an override inherits from All Reports, not from the root"* actively described behaviour the window no longer has. `Standard` stays in the registry and `[radio.voices.standard]` stays a config key. | agent, REVIEW 2026-09-06 |
| F-47 | **`--ascii` survivors in the radio panel: `▶`, `■`, `█` and `░` are hardcoded instead of taken from the glyph set.** `radio_panel.go:386/395` write `"▶ PLAYING"`, `:397` writes `"■ STOPPED"`, and `:77` repeats `"█"`/`"░"` for the volume bar. | **The tell is inside the one function.** `radioStateLabel`'s *failed* branch DOES go through the set — `d.opts().Glyphs().Fail` — while its playing and stopped branches do not, so the same function is half-wired. The set already carries `Play` and `Fill` with ASCII forms (`units.go:106/110`); **`■` has no entry at all**, so there is nothing to fall back to and a Stop glyph must be added. Visible in `setup-133x44-ascii.golden`, which captures the whole frame. | **Same class as F-37, in a surface F-37 does not name** (it lists Location Details, Alert Details, the Details chip and `ctrl+d`). It survives because the Setup ASCII test's forbidden list is scoped to *that window's* marks — `▾ █ ▲ ▼ ● ○ › —` — while the golden it checks is the whole frame, so panel glyphs are captured and not scanned. Small and clearly fixable: route the four through the glyph set and add a Stop entry. **The scan should also be widened**, or the next one hides the same way. **FIXED, and widening the scan immediately found a THIRD instance.** `Stop` added to the glyph set (`■` / `#`); the volume bar now takes `RailCar`/`Fill` and `radioStateLabel` takes every mark from the set rather than only its Fail branch. `TestASCIIFramesCarryNothingButASCII` replaces a list of forbidden glyphs with the only question that needs no list — *is anything in an `--ascii` frame outside ASCII?* — allowing `°` alone, which is a ruled exception (the real DEGREE SIGN, deliberately not U+00BA, which a screen reader announces as an ordinal). On its first run it caught `·` U+00B7 on the tape: `app/ticker.go:611` composes the Head as DATA and cannot know about `--ascii`, so the fold belongs at `tapeLine`, the one place the tape's text crosses the glyph boundary — `tickerBullet` already knew this for the separator BETWEEN items while the one INSIDE an item went unhandled. Controls: re-hardcoding `■` fails the scan on both frames. Only the two `--ascii` goldens moved (3 lines); every unicode golden is byte-identical. | agent, REVIEW 2026-09-06 |
| F-48 | **The account name has been published in the README since 0.13.0.** The `[S]` window's DUMPS line renders an absolute path — `trigger: kill -USR1 <pid> → /Users/<account>/Library/Caches/watchpost/profiles` — and `docs/img/status.png` has carried it in the public repo, and in git history, since 0.13.0. Found 2026-09-06 while refreshing the README captures. | **No gate could have caught it, and that is the point.** By the time the path is in a PNG it is pixels; the watermark gate, the NFR-8 grep and the employer-name guard all read text. **Fixed at the source rather than in the image**: `abbreviateHome` rewrites a home-prefixed path to `~/…` for both halves of the build-tag split, pinned by `TestTheDumpHintNeverShowsTheHomePath` — which was confirmed to FAIL without the fix, and which also pins that a path outside the home is left alone and that a sibling merely sharing the prefix is not rewritten. Every future capture is now safe by construction rather than by someone remembering. | **The already-published copy is a HUM LEAD decision, not closed here.** The image is withheld from the README pending a clean capture, but the 0.13.0 copy remains in the published README and in history. Options: leave it (the account name is adjacent to a public GitHub identity and is low-sensitivity), replace it going forward only, or rewrite history — the last is disruptive for a public repo and is the HUM LEAD's call. **SCOPE OF THIS ROW, ruled 2026-09-06:** it covers paths and account names only. The FIRMS key shown as `stored (…d9a6)` in `setup.png` is **NOT** in scope — *"4 chars of a real key is fine, that's why we show … in the first place"* — the truncation IS the mitigation, and a capture with a key set is a truer picture of the working state than one without. Recorded so it is not re-raised at the next capture round. | agent, REVIEW 2026-09-06 |
| F-49 | **Where the cache lives — `os.UserCacheDir()` vs a single `~/.watchpost/`.** Raised by the HUM LEAD, 2026-09-06, on seeing the dump path. Today `userCacheSubdir` builds `<OS cache dir>/watchpost/<name>`, so macOS gets `~/Library/Caches/watchpost` and Linux `~/.cache/watchpost`; config is `~/.config/watchpost`. | **Both readings are defensible.** Platform conventions put the files where each OS expects them, which is why they are there — and a cleaner is free to delete a Caches directory, which is the intent for caches. A single `~/.watchpost/` is easier to explain, easier to document in one line, and easier to remove wholesale. | **Not a 0.14.0 change.** Moving it strands every existing user's cache — including Piper voice models at about 63 MB each, which would silently re-download — so it needs a migration step and its own UAT, and the README, `docs/`, the install script and the perf protocol all name the current paths. Recorded for 0.15.0 DISCOVER with the migration as part of the work, not an afterthought. **It does not gate the PII fix**, which is about how a path is DISPLAYED and holds wherever the file lives. | **RULED 2026-09-08 (FR-7.2): the cache STAYS at `os.UserCacheDir()`, and the reason is a property, not inertia.** A cache is meant to be disposable and OS-reclaimable, and a single `~/.watchpost/` cannot be both that and the home of `config.toml`: either a listener who clears it loses their settings, or the tree stops being something any OS tool will ever reclaim — with a Piper voice at ~63 MB each lingering in it forever. **The migration was never the deciding cost.** **What the ruling DOES concede:** "hard to explain" was a fair complaint, and the cause is not the cache root — it is that config hand-rolls `$XDG_CONFIG_HOME` → `~/.config` on EVERY platform while the cache is native, so macOS has config in `~/.config/watchpost` and cache in `~/Library/Caches/watchpost`. The app is already half-XDG and nothing says so. That is now stated in `architecture.md §1.2`. **Deliberately NOT done here:** surfacing the paths in `[S]`, which already computes all three and shows only sizes. F-49 was raised *on seeing the dump path*, so displaying them is the obvious next move and it is FR-7.5's business — a full path names the user (`/Users/<name>/…`), and `app/dump.go:263` `abbreviateHome` is the owner it must go through. Ruling the layout first and the DISPLAY separately is what keeps a discoverability fix from becoming a PII regression. | HUM LEAD, 2026-09-06; ruled 2026-09-08
| F-50 | **`TestSourceStopsFastWhileMidSegment` failed once under a full `make verify`, then passed 9/9 in isolation** (6 plain, 3 under `-race`). `domains/radio/synth/synth_test.go:278`: *"reader must report the cancellation"*. Seen 2026-09-06 during the 0.14.0 release run. | **A cancellation-timing assertion that only failed under load**, in a package this release's CI work did not touch. The shape is a SUCCESS BOUND — it waits for the reader to observe a cancellation and fails if it has not happened yet — and a success bound is a race by construction, which is the distinction already recorded for the `say` bound (a failure bound can be generous because it only costs when something is wrong). | **Not chased during the release, and not dismissed.** One failure in ten is a real signal about the assertion, not about the code under it: the reader almost certainly does report the cancellation, a little later than the test allows. Wanted: re-read it as a failure bound, or give it a deadline generous enough that only a genuine hang trips it. Recorded rather than re-run until green, which is how a flake becomes permanent. **FIXED 2026-09-06, and the fake could not pose the state the test is about.** `toneVoice` returns 100 ms of audio instantly whatever the text, so BOTH segments were produced immediately and the reader could drain to a clean EOF before the cancellation was observed — the test failed for being right too early. `heldVoice` speaks the first segment and blocks inside the second until the context is cancelled, so there is a genuinely pending write to unblock. The premise is now asserted rather than assumed (*"the source never began the second segment: this test cannot pose the cancellation it is about"*) — controlled with a single-segment script, which fails with exactly that message — and the 200 ms success bound is a 5 s failure bound. **AND THE FIRST FIX WAS STILL WRONG.** It kept the original assertion — that `io.ReadAll` reports a non-nil error — and CI failed it again on macOS. That assertion is not stable: after a cancellation the stream may equally end CLEANLY, and both outcomes appeared on macOS within one commit. **UAT 81 is about promptness, not the error.** The read now runs in a goroutine and the test asserts only that it RETURNS; with the voice held open it cannot finish unless the cancel unblocks it, which is what makes it falsifiable — removing the cancel fails it with *"the reader was still going after 5s"*, verified. I had preserved an assertion instead of asking what it was for. | agent, SHIP 2026-09-06 |
| F-51 | **`TestPublisherWindowFoldsAWave` failed on one macOS CI leg while the OTHER leg passed on the same commit.** `app/pipelines_test.go:119`: *"twenty triggers inside one window publish once, got 2"*. Passes 8/8 locally. Seen 2026-09-06 on the 0.14.0 release PR. | **Wall-clock, not logic.** The test builds a publisher with a 40 ms window, fires 20 triggers 1 ms apart and sleeps 120 ms. Under CI load the wave spreads past 40 ms, a second window opens, and the fold count is legitimately 2 — the code did the right thing with the timing it was given. **Same commit, two runs, one pass and one fail is the cleanest proof of a flake there is**, and it needs no reproduction argument. | **Same class as F-50 and the same fix: a real-time SUCCESS bound.** `platform/sched` already has a fake clock (`awaitWaiters`) built for exactly this during 0.14.0; the publisher takes its window as a field, so it is injectable. Not done during the release because pipelines is untouched by this work and a clock change deserves its own review. **FIXED 2026-09-06 without touching production or adding a clock.** The wave is sent back-to-back — microseconds — into a 500 ms window instead of twenty triggers spaced a millisecond apart into 40 ms, so the wave no longer nearly fills the window it is meant to sit inside. Both sleeps become `awaitRuns`, a failure bound that waits for the count to REACH n. Waiting longer is safe by construction and that is what makes "exactly 2" assertable without a clock: once the wave is sent, no trigger remains to publish, so the count cannot drift past. Controlled by removing the wave — the wait then fails with *"publishes never reached 2 (got 1)"*. | agent, SHIP 2026-09-06 |
| F-52 | **`TestSayVoiceOnDarwinNarratesHostileTextSafely` fails on GitHub's macOS runner: `/usr/bin/say` is present and produces 236 bytes for a whole sentence.** The test asserts at least half a second of audio (22,050 bytes). Passed on one macOS leg and failed on the other, same commit. | **Present is not functional.** The test's only guard was `os.Stat("/usr/bin/say")`, which the runner satisfies while rendering essentially nothing — a headless box with no usable voice. Failing there says nothing about the property the test exists for, which is that shell metacharacters in product text are INERT (narrated from a file, never interpreted). | **FIXED with a control render.** A benign sentence is spoken first: if THAT does not render, the host cannot speak and the test skips with a reason naming the byte count. If it does, the hostile text is compared against it rather than against an absolute — hostile must render like ordinary. That separates "this box cannot speak" from "our input broke it", which is the distinction the absolute threshold could not make. | agent, SHIP 2026-09-06 |
| F-53 | **CLOSED 2026-09-07 (0.15.0 B4).**  `platform/bodymemo` is the one implementation: a bounded, hash-revalidated parse cache, generic over the key and the parsed value.  `tileMemo` keeps what is FIRMS's own (the per-source split pitch) and `boxMemo` is now four lines and a name.  The consolidation was measured against allocation pins built BEFORE it, in the provider packages where the memos live — a hit allocated 0 before and allocates 0 after, and both pins were watched failing at 1 with a planted defensive copy.  The package's own tests state the three properties OQ-9 ruled.  ORIGINALLY: **Two memo types, one pattern: `tileMemo` (fire/firms) and `boxMemo` (seismic/usgs) have byte-identical `stats` and the same shape around it.** Surfaced by the duplicate-body sweep during the #7 consolidation, 2026-09-07. | **Deliberately NOT folded into 0.14.1.** Unlike the six that were, this is not a policy stated twice — it is two independently-evolved caches that happen to share a shape. Merging them means a GENERIC memo type, which is a design change: it touches two domains, changes allocation behaviour on a path the frame budget pins, and would need its own before/after numbers. | **0.15.0, with performance measured rather than assumed** — HUM LEAD, 2026-09-07: *"I agree it's a design change, and we should also see how it affects performance."* The frame allocation pins (`make alloc-budget`) and the memo hit/miss gauges are the instruments; a generic memo that costs allocations on the tick is worse than two small honest ones. | HUM LEAD, 2026-09-07 |
| F-54 | **The Code Quality red-team lens does not ask whether an operation is implemented more than once.** Ten lenses across four rounds on 0.14.0, and none of them found that `Installed` was written twice, that the closed allowlist and the last-resort voice were written twice, or that two domain packages each declared `maxListLen`/`maxFieldRunes` with four differently-named helpers. Issue #7 shipped because of it. | **HUM LEAD, 2026-09-07:** *"this is the kind of activity I expect a Code Quality red team agent to be looking for and asking during build and review phases."* The check is mechanical and cheap — normalise every production function body, hash it, report collisions; then pairwise-similarity for the same thing done DIFFERENTLY, which exact matching cannot see. Nine identical groups and one two-owner sanitisation policy came out of a hundred-line script that had never been run. | **Wanted: it goes in the lens's brief**, with the script as a tool the lens runs rather than a thing it reads for. The framing that earns its place: *two implementations of one operation is a defect that has not happened yet* — the day one is corrected and the other is not, they disagree, and both look right in isolation. A red team reading only for the correctness of what is written cannot see the risk in what is written twice. | HUM LEAD, 2026-09-07 |
| F-57 | **Eleven tokens cannot be measured by the AA register as it is built, in three classes.** Found by the completeness gate FR-8 added (`platform/render/aa_completeness_test.go`), which crosses the token vocabulary against `aaPairs()` — the register itself could never have found them, because the AA test iterates the same list `withAA` lifts and is therefore a tautology. | **Each class has a MECHANISM, checked rather than assumed.** (a) **Composites** — `chip.key`, `chip.key.muted`, `chip.flash.up/down` are one value carrying a foreground, a background and a weight (`chip()`), painted in a single escape by `sgrRaw`; the register's `fg x [bg]` model is neither. (b) **Gradient stops** — `title.grad.start/mid/end` are interpolated BETWEEN, so no stop is what is painted, and the wordmark is large display text whose AA bar is 3:1 rather than 4.5:1. (c) **Decorative bars** — `radio.spectrum.low/mid/high` ARE expressible against the window ground, but they carry nothing the frame does not also say in words (the play mark, the volume), so their threshold is 3:1 and whether they owe anything is a colour ruling. `radio.bg` is NOT in this row: it is SGR 49, the terminal's own default background, and there is no colour there to measure at all — that exemption is permanent. | **A second check, not a wider register.** (a) wants a composite splitter measuring a chip's own fg against its own bg; (b) wants the interpolated values sampled at a 3:1 bar; (c) wants a HUM LEAD ruling first, since colour is his pass. All three are cheap individually and none of them is the register's shape, which is why they are exempt rather than bodged in. | FR-8, 0.15.0 B6 |
| F-56 | **go-studs has no FIT column, and it should.**  `ColumnDefinition.Width == 0` means **fill** (`data_table_row.go:539/609/924`), so a table of unsized columns stretches to the terminal and puts a hand's width of air in the middle of every row.  `table.go`'s `AdvancedDataTable` sizes from HEADER lengths and distributes the slack — also not a fit, and it is the component the standing rule says not to use.  There is no path in the kit that measures the cells. | **Implemented locally as `render.fitColumns`** (0.15.0, `platform/render/detail_table.go`): every unsized column is set to its own widest cell before the definition reaches the kit.  Twelve lines, and it is the thing that let the fire tables pull in to the width of what is actually in them instead of running into the scroll rail. | **Upstream candidate (M6).**  `Fit bool` on `ColumnDefinition`, sized from the row data the definition already carries — the kit has the rows, so it can measure them without the caller doing it.  Until then the local helper is the one owner, and any second detail table gets it for free. | HUM LEAD, 2026-09-07 ("doesn't the kit have a 'fit' property?") |
| F-55 | **MEASURED BELOW THE FLOOR (REVIEW stress pass, 2026-09-08): the app does not degrade, it OVERFLOWS SILENTLY.** At a 20x5 terminal the frame renders **57 cells wide and 25 lines tall** — nearly 3x the width and 5x the height of the terminal — with no clamp and no notice; 1x1 and 40x10 produce the identical frame, so nothing consults the size below the floor at all. It does NOT panic: every window in the enum renders at 24x6, and an empty location and a nil snapshot both render. So what the size contract owes is a NOTICE, not crash-proofing. This is new evidence for the ruling below, whose own measurements were about scroll reachability AT 80x24 rather than below it. **RULED 2026-09-07 — the answer is a MINIMUM TERMINAL SIZE, not per-window chrome.**  HUM LEAD: *"we might have to adopt something like btop's minimum terminal size setting to allow for certain controls, or when it's a narrow terminal we pop a small modal telling the user they need to make their terminal the optimized size in order to do certain things.  This will be ABSOLUTELY true for Broadcaster UI."*  So the three windows named below are not three defects waiting on a scroll fix — they are the measured case for a size contract, and their baselines come down when it lands.  **What the design owes:** the floor itself (80x24 is the documented one, and is where these fail), which controls require more than the floor, whether the notice is a modal or a band, what it says, and what happens to a window already open when the terminal shrinks under it.  **Where:** with Broadcaster UI, whose operator surface is all focus lists.  ORIGINALLY: **A window whose scroll follows the focus cannot show its own head at 80x24.** Measured 2026-09-07 by the FR-5 reachability property, across every window in the enum: three carry unreachable lines and it is one mechanism, not three defects.  The scroll can go no further up than the offset that keeps the focused row on screen, and nothing else moves it — so the body above the FIRST focusable row is lost whenever that row sits more than a window's height down.  What is lost is each window's identity: the relay-fault window's `*** ERROR ***`, the two Settings group headers (`WATCHPOST RADIO - CORRESPONDENTS`, `WATCHPOST UI`), and the ctrl+d window's title plus its `These are FABRICATED` caveat — the sentence that tells a tester the alert they are about to inject is not real. | **The counts are pinned, not accepted.**  `modes/tty/modal_reachability_test.go` carries the baseline — setup 2, relay-fault 1, debug 7, every other window 0 — and ratchets BOTH ways, so a window that improves must take its number down and cannot drift back.  Two adjacent defects were fixed rather than pinned (`ac69622`): the offset counted in unwrapped coordinates, and the shipped ctrl+d window had no focus and therefore no scroll at all. | **A HUM LEAD ruling, and there are three options.**  (a) Pin the head as chrome the way the footer is already pinned (`ScrollPanelFooter`), which recovers each window's identity and nothing else.  (b) Give the body a key that scrolls free of the focus, which recovers everything and spends a binding — see F-26 on how few are left.  (c) Accept it at the floor and say so.  Worth ruling before Broadcaster, whose operator surface is all focus lists. | FR-5 property, 0.15.0 B3 |
| F-43 | **A tone sounded three times with no words and no marquee callout.** HUM LEAD, UAT 2026-09-06, on a real burst. **A tone is a promise of words, and nothing in the tree pins that promise.** | **A reachable path, offered as consistent with the report rather than as the diagnosis.** For a takeover `runCue` returns early — the band is cued from INSIDE the read, per line (`cueFor`) — so the order is tone → first line's cue → words, and a failure BETWEEN the tone and the first line yields exactly what was heard: tone, no callout, no words. The only thing in that gap is `openTone`'s closing `holdRest`, which holds for what is left of the tone and returns false if `awaitAir` finds the sequence cancelled; `readScript` then returns at once, before any part. Three tones = three bursts each dying in that window, which fits arrivals in quick succession pre-empting one another. NOT established: what cancelled them. | **AND IT IS QUIETER THAN IT WAS, BY MY HAND.** Before 2026-09-06 a cut-short read came home `Failed` and, on an empty schedule, raised the relay-fault window; I-2 made the cut-short `Routed`, which is right — pressing esc must not claim the relay is dead — but it also means THIS failure now says nothing at all. Two things wanted: a pin that a sounded tone is followed by at least one spoken part (the invariant the report violates, and there is none today — `app/tone_latency_test.go` measures the tone's LATENCY, not that words follow), and a trace line where the read gives up after the tone, so the next sighting names its own cause instead of needing this reconstruction. **BOTH LANDED (`c7ace52`)** — `app/tone_promise_test.go` pins that a sounded tone is followed by words, and `openTone` traces `read:gaveup:after-tone` with the tone's class, its duration, the part count and the context's error. The pin's first version PASSED WHILE PROVING NOTHING: `classVoice.tone()` returns a zero duration, which sends `holdRest` down its `awaitAir` branch and never poses the window at all; only its control caught that, and `tonedVoice` fixes it. **Not reproduced since** — HUM LEAD, 2026-09-06, through a subsequent alert burst. That is a trap armed and unsprung, not a fix: nothing was repaired, so the row stays open until either a sighting produces a `read:gaveup:after-tone` line naming the cause, or a decision is taken to close it unexplained. | **CLOSED UNEXPLAINED, HUM LEAD 2026-09-08 (FR-6.6).** *"If we have no reproduction this far — after lots of UAT sessions — let's log it as something that was observed once, but cannot be chased until we have a reproduction. Otherwise we're just chasing vapors."* The ruling is on the EVIDENCE, not on the severity: one sighting, many UAT sessions since, and a mechanism that is reachable but unproven. **What stands guard in its place:** `app/tone_promise_test.go` pins that a sounded tone is followed by words, and `read_script.go:162` traces `read:gaveup:after-tone` with the tone's class, duration, part count and the context's error. **REOPEN ON A TRACE LINE, not on a memory** — the next sighting names its own cause instead of needing a reconstruction, which is the whole reason the trap was armed. Note for whoever reopens it: the pin's first version PASSED WHILE PROVING NOTHING (`classVoice.tone()` returns a zero duration, which never posed the failure window), so re-validate the instrument before trusting a green run. | HUM LEAD, UAT 2026-09-06; closed 2026-09-08
| F-38 | **Nothing verifies at the ARTIFACT level that the injector is absent.** `app/inject_test.go` asserts on the *text* of two source files; `release-matrix` and the release workflow build and publish without ever inspecting the produced binary. A `GOFLAGS=-tags=watchpost_debug` in the runner environment, a future `Inject` defined in a third file, or a scenario list wired from another package all pass today's guard. | **The property holds right now** — the BUILD-exit red team built a default binary and checked it: `go tool nm` shows zero `Inject` symbols and `strings` zero scenario labels, so the P10-08 exemption stands. What is missing is that the *pipeline* never does that check. The test's own comment names `lint-imports`/`lint-watermark` as the right shape; take its advice. | Before the tag, if it is cheap: one `release-matrix` step failing if any artifact contains `Injected Test Location`, plus a repo-wide grep gate forbidding `.Inject(` outside a `watchpost_debug`-tagged file. | Red team BUILD exit round 3 (INFOSEC) |
| F-30 | **CLOSED 2026-09-07 (0.15.0 B4, FR-3.3).**  Both mechanisms the row named are gone: an unfixtured window is now a **FAILURE**, not a `t.Skipf` — watched failing by planting a twelfth modal with no fixture, which reported `modal-12 draws nothing at fixtureFor` — and the nested-struct set is **DERIVED** rather than named, so a fourth window's state is perturbed without anyone remembering.  Deriving it found a real gap on its first run: `cfg.Version` reaches the About window's frame and is not in the key.  It is EXCUSED with a reason — nothing writes it after construction — and the excuses themselves are guarded, so one naming a field that no longer exists fails rather than silencing quietly.  The rest of `cfg` IS walked, and must be: Cast, Tones, Radio, Spectrum, Units and Clock are all written while the app runs.  ORIGINALLY: **ANSWERED IN 0.14.0, kept for what it does not cover.** `modes/tty/memo_completeness_test.go` (`TestTheMemoKeyCoversEverythingTheFrameShows`, headed "F-30, mechanised") now derives the fields by reflection and asserts that two dashboards which RENDER differently cannot share a memo key — the exhaustive shape this row asked for, rather than the enumerated table. **The original defect:** a window whose moving state is not in the key is rendered ONCE and that frame replayed for as long as it is open; the relay-fault window took THREE UAT rounds to find because everything underneath was correct and the listener saw a still picture. | **What the guard still does not reach**, and why the row survives its own fix: it SKIPS a window whose fixture draws nothing, and it descends only into `relayFault`, `debug` and `setup`. A window added with a cursor and no fixture is still frozen by default. (It reported one coverage hole at BUILD exit — `modalVoice`, an enum member retired with the voice chooser and never assigned; deleting it took the guard to 11/11.) | **RE-VERIFIED 2026-09-07 (0.15.0 DISCOVER) — the row stands, and the guard's green number does NOT mean what it looks like it means.** The run reports 11/11 with zero skips, which describes TODAY'S windows and says nothing about the guard. Both mechanisms this row names are confirmed in the source: `memo_completeness_test.go:49` `t.Skipf`s an unfixtured window rather than failing, so a new window is silently uncovered; and `:122` descends into exactly three named nested structs (`relayFault || debug || setup`), so a fourth is never perturbed. **The work is to make an unfixtured window a FAILURE and to derive the nested-struct set rather than name it.** Before the next window with a cursor is added, give it a fixture. Broadcaster's operator surface is all cursors. | UAT 2026-09-05, three rounds; mechanised at BUILD exit |
| F-27 | **Inter-card transitions.** SCRIPTS WRITTEN AND PINNED (`transition/resume`, `transition/masthead`, `a3c495b`); the WIRING is what remains, and its design is settled below. | **RULE (MVS-D-80, HUM LEAD 2026-09-05).** Fires when something that interrupted the programme leaves the air and the programme resumes: an alert takeover, and a `[w]` read including one cut short by `[esc]`. **A DUCKED RELAY GETS ONE**, with the "already in progress" clause — the programme never stopped, which is what that reading exists to say. Does NOT fire location-to-location (*"the location scripts already announce their location"*) nor between alerts inside a burst (intra-card, MVS-D-77). **PLACEMENT: at the duck (HUM LEAD, option A)** — the lift is the one point that sees the takeover, the `[w]` read and the relay alike, and `mastercontrol` already owns duck/restore. | **THE DESIGN, AND THE HAZARD THAT SHAPES IT.** It must NOT be spoken from inside `director.release` / `mastercontrol.takeBack`: that path runs under the arbiter's lock and is the exact function F-D5 was filed against (*"the dip and the claim were not one critical section"*), so speaking there means reentering the arbiter while it holds that lock. **The transition is a NARRATION, so make it one:** enqueue it while the interrupting read is still on air, and the duck stays down by itself — `takeBack` already refuses while anything is first or resumable, so the bed lifts when the TRANSITION ends. No lock changes, no reentrancy. **The hazard to design against is the duck-bounce**: enqueue it after the read has ended and the bed lifts, then dips again for the transition, then lifts — which is precisely the dip-lift-dip MVS-D-67 removed and MEASURED. It therefore has to be queued behind the read, or spoken before `releaseBed`'s `unhold`. Needs one new seam: whether the bed is a LIVE relay (`mode == "live"` on the deck) for the clause. `programmeReturnLine` is renamed away from `mastercontrol.resumeLine`, which is a different thing. | **Its own task with its own red team.** It is new behaviour on the duck path, which has a defect history (F-D5, MVS-D-67) and is where a mistake is heard rather than seen. | HUM LEAD, UAT 2026-09-05 |
| F-26 | **A "STOP ALL" control — return Observer to no active reads or relays.** Raised by the HUM LEAD while ruling the `[esc]` double-read (UAT 2026-09-05). Today there is no single way to silence everything: `[space]` stops one location, `[esc]` ends a `[w]` read, `[M]` holds hazards, and a tuned relay keeps playing through most of it. A listener who simply wants quiet has to find each one. **Alerts and takeovers keep working normally** — this is "stop what I started", not standby. | It is deliberately NOT a confirmation on `[esc]`: the HUM LEAD ruled that key controls must feel instant ("that's the whole point of using a TUI"), and a modal in that path contradicts it. So it wants its own binding, and the binding is the open question — the obvious letters are taken and a chord risks the same terminal-interception problem noted for `ctrl+d` (F-21). | Needs a key-binding decision before it can be built. Worth pairing with F-21's diagnostics window, where "what is currently making sound" is a natural thing to show. | HUM LEAD, UAT 2026-09-05 |
| F-24 | **A MASTHEAD to cover the resumption gap.** Coming back ON AIR from standby, the first card has to be composed and rendered before anything can be said — about a second on macOS, and the Reader falls back to a 5 s hold when a render yields nothing. So the moment a listener says "go" is the moment of the longest silence, which reads as the station being broken at exactly the wrong time. A station ident played on resumption fills it. | **The design already solves this problem once, which is why the shape is known.** MVS-D-72's attention tone is not decoration: the head renders DURING it, so the words begin the moment the tone ends rather than a render later, and `readScript.openTone` is where that overlap lives. A masthead is the same trick for a different boundary — sound something the station already has while the first card is prepared behind it. It wants the same treatment: a fixed clip, rendered ahead or synthesised once and cached, never composed on the resumption path. | With Broadcaster, where an operator presses GO and expects the station to speak. Observer inherits it on unmute if the same gap proves audible there. | HUM LEAD, 2026-09-05 |
| F-23 | **A listener's own weather station as an observation source.** The Lone Pine defect is the business case: a location with no NWS station within twenty miles now reads its own grid FORECAST — the most accurate number available, and still modelled rather than measured. Hobby station kits are cheap and common, and an endpoint that accepts a listener's own observations would make those locations genuinely measured. **It is complementary to Broadcaster rather than incidental to it:** a Broadcaster reading hyper-local *measurements* for its service area is a different product from one reading a grid model, and that is the claim "hyper-local weather radio" actually needs. | The seams already exist and this is why the shape is right: `Conditions` carries `Source.Provider`, `ModelOrStation` and `DistanceKm`, `harmonize` fills field-by-field with `FillFrom` provenance, and the card already distinguishes measured from modelled in words. A listener's station becomes another provider at distance ~0, and everything downstream — the DIST colour, the grid-forecast note, the twenty-mile bound — keeps working unchanged. | Well after Broadcaster (HUM LEAD). Recorded now because the defect supplied the justification, and because it argues against any future shortcut that assumes NWS is the only source of an observation. | HUM LEAD, UAT 2026-09-05 |
| F-22 | **The band's "one owner" rule is structural and nothing enforces it.** D-1/T2.3 hold that only `mastercontrol` writes the band — it was written from `app/director.go` AND `app/ticker.go` before, two files constructing the same takeover message, and the pair drifted. **m50 claimed to guard this and did not.** Re-anchored at T3.8 to send `tty.TickerBreakingDoneMsg` directly instead of through `mastercontrol.clearBand`, it SURVIVED: both produce the identical observable message, so no test can tell them apart. Its previous CAUGHT verdicts came from the early return its old form also introduced — it was passing for a reason other than its stated rule, and only moving it revealed that. The mutant is retired rather than left as a green line that measures nothing. | A rule about WHICH CODE may do a thing is not observable in behaviour, so it belongs in a gate rather than a test — the shape `lint-imports` and `lint-watermark` already have. Roughly: outside `app/mastercontrol.go`, no file may construct `tty.TickerBreakingMsg` or `tty.TickerBreakingDoneMsg`. Cheap to write, and it self-tests the way the other two do (a control that must fire). | Before the Broadcaster adds a second writer of the band — 0.15.0 opens exactly that door, since an operator surface has its own reasons to touch a callout. | T3.8, found by re-validating a moved mutant |
| F-21 | **A `ctrl+d` diagnostics window — and the injection half is a SEPARATE feature with the opposite shipping answer.** The HUM LEAD's framing is the important part: this should let *a human user* confirm whether the app is working, the way the weatherUSA outage needed confirming — the program was correct and an upstream service was failing **while appearing healthy** (HTTP 200, `audio/mpeg`, correct ICY headers, ~19 KB/s of well-formed MP3, decoding without one error, and total silence). Every signal the app checks was green and the listener heard nothing. **Two features are hiding in one idea, and conflating them is how the hazard gets in:** (a) **diagnostics** — read-only checks a listener can run against reality: is this mount producing audible audio (the RMS instrument already exists as `TestMountDiag`), are the feeds answering, when did each last update. This is genuinely worth SHIPPING, because it answers "is it me or is it them" and today nothing does. (b) **injection** — fabricating an alert to exercise the takeover on demand. This must **never** ship: a screenshot of a fabricated tornado warning is indistinguishable from a real one, so it is build-tagged out of release binaries rather than gated at runtime, because a runtime gate is one config mistake from being reachable and a build tag is absent from the binary. | **The injection seam is the whole value of (b).** It must enter where a real alert enters; an injector that shortcuts validates the machinery and says nothing about the wiring — the same failure as three tests that passed over an inert Settings row by calling the cycle function instead of pressing a key (D-12). **And the logger is not currently cheap:** `writeRadioDebug` does open/write/close **per line**, on the pump's serial goroutine, and T3.6 multiplied the line count from one per event to three-to-ten. Buffered writes with a periodic flush, into `~/.watchpost/debug-logs/<timestamp>.txt`, so no variable and no path need be known. `ctrl+d` rather than `[D]`: a printable key types into any text field. Note `ctrl+d` is EOF in many terminals and some multiplexers grab it first — worth confirming on the target setup before committing. It is a user-facing surface and carries those obligations: `aaPairs` registration, keyboard-dismissible, focus without trapping. | **(b) before T3.7's UAT if that swap is to be verifiable at all** — wiring the arrivals replaces the working takeover path, and validating it against whatever weather occurs is not validation. (a) can follow on its own merits. | HUM LEAD, UAT 2026-09-04 |
| F-20 | **The severe window's category heading was removed, and could earn its place back.** It read `Advisories — 14 active` directly above `14 Total Category Events` — the same number twice, differing only above the 500-row cap. The HUM LEAD named two things that would make a heading worth the line: a **windowed running total** ("14 active, 37 in the last 24 hours"), which needs history the app does not keep; or a **sub-type breakdown** within the tab (`Advisories — 11 active | Air Quality Alerts — 4 active`), which needs the product name carried onto the row and a grouping pass. | Neither exists today, and shipping a heading that restates the count below it is worse than no heading. | 0.15.0, or whenever the window next gains a grouping concept. The running total also wants a retention decision — how long is "recent", and where is it stored. | HUM LEAD, UAT 2026-09-01 |
| F-58 | **Lookup takes no typed input on a freshly seeded install.** Open Lookup with `l`, type `Vista, CA`: against a **warm config (one location) the box echoes instantly (+0.0 s) and Enter opens Details (+0.0 s)**; against a **fresh HOME (the 50-location seed) the echo never arrives in 25 s and nothing arrives in the 120 s after Enter.** The window itself opens fine (`Search:` at +0.0 s in both). This is what fails the journey's two Lookup steps, which have been intermittent since 2026-09-06. **Ruled out with evidence, not assumption:** not the disk (270 GiB free — the 2026-09-06 cause), not resolution (`Vista, CA` resolves from the embedded index in 0 ms, no network), not this release's render changes (the same binary passes on a warm config), and not a stale Setup modal (the probe presses `w` and requires `Total Category Events`, which only the severe window draws, before it starts). **Not the seed finishing, either: a 120 s settle changes nothing**, so "the app is busy and recovers" is wrong. | **THE CAUSE IS NOT KNOWN, AND ONE PLAUSIBLE READING IS ALREADY DISPROVEN.** A forced repaint appeared to show the box still holding its `Type a city name or ZIP code.` placeholder, which reads as "the keystrokes were dropped" — but **the identical dump appears in the warm run where the echo is found 0.0 s later**, so it is one chunk of a repaint, not a screenshot. Whether the keys are dropped or merely unprocessed is therefore OPEN, and F-44's warning applies: `expect_out(buffer)` is not a frame. Finding out means profiling the update loop under a 50-location seed (`WATCHPOST_DEBUG_PPROF`), not reading more dumps. | **Its own investigation, instrument first** — the repro is committed as `scripts/quality/lookup-stall-probe.expect` and takes ~40 s against the journey's 5 minutes, so the next attempt starts from a measurement. **The journey's bound must NOT be widened to absorb this**: 4×25 s already fails honestly, and stretching it converts a measured stall into a green tick — the same call F-44 made for the 11.4 s `space` read. HUM LEAD 2026-09-08: document and proceed, fix only if cheap; it is not cheap. | 0.15.0 B6, chasing two journey failures |
| F-59 | **Test-code duplication: 17 unexplained groups the success metric does not count.** `go run ./tools/dupes -tests` reports 18 groups at the same 25-node floor, 1 ratified and 17 not — the largest being five copies of `TestDeclarationSetUnchanged` at 88 nodes, which is per-package by necessity and wants judgement before work. Found at the 0.15.0 BUILD-exit red team, against a gate that had been reporting "0 unexplained" while never having looked at a test file. | **Metric D's scope is PRODUCTION, and that is now stated rather than defaulted** (`06_docs/duplicates-ratified.md`). HUM LEAD 2026-09-08: *"(a) is the metric that matters for success; (b) is an internal quality metric that we should work down, but that's a secondary or tertiary metric since it pays us in productivity later, but doesn't necessarily affect the end-user experience / quality of the application."* | **Worked down over time, never gated and never a release criterion.** The number is re-derived on demand by the same instrument, so this row needs no maintenance to stay true. | Red team, 0.15.0 BUILD exit |
| F-60 | **The README promised a voice-download cap that does not exist, and the sentence was corrected rather than the code.** It said *"Watchpost fetches at most two voices in the background per session, so a hand-edited config cannot quietly pull hundreds of megabytes on launch."* There is no such cap: the three install paths (first use, the background goroutine on a cast change, and the preview) all serialise on `installMu` — one at a time, never a count — and `renderSlots` (2 off macOS, 3 on it) is RENDER concurrency, not downloads. A config naming distinct voices across the cast installs them all at ~63 MB each, as each is first heard. Found by the VALIDATE README content audit, 2026-09-08. | **The README now describes what the code does** (on first use, never at launch, one at a time), because documenting a protection nobody built is worse than not having it. The claim's INTENT is still defensible: a listener who hand-edits nine roles to nine voices has asked for ~570 MB without being told. | **A ruling, not a fix**: is a per-session install cap wanted, and if so what happens when a read needs the voice beyond it — fall back to the inherited voice, or install anyway and say so? Both answers are listener-facing, so neither is the agent's to pick. | VALIDATE README audit |
| F-61 | **The spoken fire report has no age bound: an incident list an hour old is read in the PRESENT TENSE.** If WFIGS answered an hour ago and is down now, `IncidentsAsOf` is old-but-nonzero, so `IncidentsKnown` is true and the read says "there are currently N named incidents within a 31 mile radius". There is no staleness bound anywhere on fire data — `grep -rn 'stale\|MaxAge' domains/fire app/fire.go platform/snapshot/merge_fire.go` returns nothing — and the detail modal checks only `AsOf.IsZero()`. Found by the VALIDATE red team, 2026-09-08, as the residual beside the per-feed fix. | **Not a blocker, and the distinction is the point.** Every report head already says *"Data for this report may be delayed or incomplete"*, which covers STALE DATA. It does not cover a positive assertion of ABSENCE — "there are currently no named incidents" is not delayed data, it is a claim about the world — which is why the never-answered case WAS a blocker and this is not. Pre-existing; not introduced by the per-feed stamps. | **A ruling on the bound**: how old is too old for a fire list to be read in the present tense, and what does the read say past it — an age ("as of an hour ago") or a refusal. Both are listener-facing. Note the answer likely differs per feed: HMS refreshes every 10 minutes, WFIGS is an incident filing. | VALIDATE red team |
| F-62 | **The radius field renders "Within [0] mi" while 0 MEANS "All locations".** `setup_form.go:151` prints the typed buffer; `alertRadiusChoice()` maps 0 to All (0.12.0: 0 = global). So a listener who types `0` reads a promise of near-silence and gets every severe event nationwide. | **Errs toward MORE alerts, never fewer**, which is why it is filed rather than fixed under time pressure: the failure direction is safe. Pre-existing, not from this release. | Either refuse `0` in the field, or render it as "All locations" the moment the buffer reads zero so the row and the stored meaning agree. A HUM LEAD wording call. | VALIDATE red team |
| F-63 | **Nothing in the phase-exit sequence reads the branch's CI state, so a RED run sat unexamined across two review phases.** The last CI run on `feature/0.15.0-pre-broadcaster-ui-improvements` (2026-09-08) failed `race` on `ubuntu-latest` while the `macos-latest` leg of the same run passed. BUILD exit, REVIEW and VALIDATE all reported ALL GATES GREEN, truthfully — every one of those gates runs on the developer's machine, which is macOS. Found at SHIP, 2026-09-09, by looking. | **The defect it was hiding is fixed and the finding is the process gap, not the test.** `06_docs/required-gates.txt` is the list an exit is checked against and it names only local gates, so a green `make verify` reads as complete when a whole platform has not been asked. 0.14.0 recorded the same class of surprise from the other direction — its PR was the first Linux run in eight days and took five rounds — and the lesson was not converted into a check. | Add the branch's last CI conclusion to `required-gates.txt` as a gate an exit must read, with the run id and its platform legs cited the way every other gate cites its evidence. Cheap: one `gh run list` per exit. | SHIP |
| F-64 | **The mutant corpus was running against `go test`'s DEFAULT 10-minute timeout, and crossed it.** On 2026-09-09 the same commit (`02fb023`) passed `mutant-check` in the `pull_request` run and died in the `push` run with `panic: test timed out after 10m0s`. The passing run took **8m06s** on `ubuntu-latest` for 172 mutants; the same corpus takes **250s** on the developer's machine, so the margin was invisible locally — local has more cores. Adding one mutant (`mK8`) at SHIP is what crossed it. | **Fixed here** with an explicit `-timeout 40m`, ~4x the SLOWEST run observed, with the measurement written into the Makefile so the next person adding a mutant does not rediscover it. **The fix proved load-bearing on the very next round:** the two green runs took **396 s** and **602 s** on `ubuntu-latest` for the same commit, and 602 s is **two seconds** under the default it would otherwise have been measured against. Three observed Linux runs span 396–602 s — a **1.5x spread** on one commit and one platform, which is what makes a clock-limited gate unreliable rather than merely slow. **The failure mode is what makes this worth a row**: a gate whose limit is the clock does not report on the code, and it fails at random — the same commit was both green and red within four minutes. | **The residual is the trend, not the flag.** The corpus grows every release (171 -> 172 in this one alone) and nothing measures its runtime against the budget. Either have `mutant-check` report its own duration against a stated ceiling, or accept that the ceiling is re-checked by hand at each release and say where. | SHIP |
| F-19 | **A cancellation is filed away from what it cancels.** A `Severe Weather Statement` follows up an active warning and often downgrades or cancels it, but `globalfeed.Supersedes` requires `newer.Product == older.Product`, so an SVS never supersedes the Tornado Warning it references. The cancelled warning stays listed under Warnings until its own `Expires`, while the statement that cancelled it sits in Spec. Statements. | **Not a regression** — before MVS-D-57 the SVS was not shown at all, which was strictly worse. Fixing it means teaching supersession about cross-product references, which touches the alert-lifetime rules. | A post-review follow-on with its own scoped red team, after the Director redesign. | 0.14.0 round 3, classification review |
| F-18 | **The AA gate shrinks silently whenever a token is added.** `platform/render/contrast.go:aaPairs` is a hand-maintained register of painted pairs, and a token absent from it is never measured — it does not fail the gate, it is simply not in the gate. **`ConfirmBG` was one of them and was genuinely failing**: an independent review measured Tokyo Night's confirm tile at **4.17:1**, under the project's own 4.5 floor. It is registered now and lifts to AA. Nine remain unregistered: `KeyChip`, `KeyChipMuted`, `GradStart/Mid/End`, `SpectrumLow/Mid/High`, `ChipFlashUp/Down`, `RadioBG`. **This is not theoretical**: `EventCatForecastBG` was added in `349401c` and would have shipped "passing AA" without ever being measured, caught only because the author went looking. | Whether each of the twelve *needs* a check is a separate question and mostly a colour judgement — `KeyChip` looks like a self-contained fg+bg chip the register's fg/on model cannot express, the spectrum bars and chip flashes are plausibly decorative, and `RadioBG` has no non-test user I could find. None is a known contrast failure; the gap is that nothing would tell us if one were. | **Before the next token is added** — which the read-lineup redesign will do. A completeness test asserting every declared `Token` is either registered or on an explicit exemption list with a reason, so forgetting becomes loud instead of silent. | 0.14.0 round 3, found while verifying the author's own omission |
| F-16 | **A new headline waits for its lane's tape to wrap.** Each lane parks its scroll offset when the rotation leaves it, and the offset is kept even when the lane's alerts change — so a warning issued while the rotation was elsewhere comes back parked deep in the old tape and is not on the band until the tape wraps. Reproduced: the lane returns 384 columns in, showing a filler alert, with the new warning absent. | **Two attempts were reverted, each worse than the defect.** Resetting on any change to the lane restarted a busy lane on nearly every publish — a red team measured 12 % of the tape ever reachable while the band's counter still said 30. Restarting only on a new head measured 47 % on a corrected sweep, still under the 90 % bar. Neither the fix nor the measurement can currently be trusted, and the symptom is milder than either regression. | **Its own DISCOVER**, with the ticker's geometry and rotation modelled and validated FIRST — every fixture written for this so far has been invalid (wrong lane measured, a 35-cell tape, a churn model that could not distinguish the two rules). The instrument comes before the fix. | Red-team BUILD exit round 2 |
| F-15 | **A `make lint` target** wiring `golangci-lint` and `staticcheck`. Neither has ever run as a gate here; the former is red on one pre-existing `QF1001` outside this feature. | Ruled out of 0.14.0 scope (MVS-D-45, E-11). | 0.15.0. | 0.14.0 PLAN |

## Closed at 0.14.0 BUILD exit

The `[S]` window's kit migration, the frame performance pass, the ticker lane rotation, the alert
priority path, the Settings save paths, the plaintext boundary and the modal-ground contrast register
were all carried items or red-team findings closed in this release — see
`02_features/multi-voice-support/08-reports/red-team-build.md` for the disposition of each.
